---
title: "Daily: 2026-04-17"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [autopatrol, inference-api, external-api, engineering-process]
workstreams: ["§1", "§3"]
created: 2026-04-17
updated: 2026-04-27
author: kb-bot
---

# Daily: 2026-04-17

## Summary

*(filled in by [[skill-daily-wrap|/daily-wrap]] at EOD — one paragraph on shape of the day)*

## Closed Line Items

- **§1 fix 500 error on motion-plus model** — Root cause: `compute_frame_differences` called `cv2.absdiff` without verifying both frames shared `(H, W, C)` shape, and any OpenCV error fell through to the `500 Internal Server Error` default in `_inference_exception_as_json_response`. Fix: added `FrameSizeMismatchException` + shape check in `inference_context.py`, mapped it to `400` in `common.py`, added regression tests, and updated the partner motion-plus doc. PR #56 merged to `develop` (commit `f3ea6623`), dev deploy green across all three region/account targets. Full write-up: [[2026-04-17_v5-motion-plus-500-fix]].
- **§1 verify images reach every v5 endpoint successfully** — End-to-end dev verification via the new `ebus-dev` key: `GET /v5/models` returned 200 with all 7 models visible; `POST /v5/detect` returned 200 for `model_id: "intruder"` (1 frame) and `model_id: "motion-plus"` (2 frames, confirming the fix above is live on dev); `/docs` with HTTP Basic Auth returned the Swagger HTML. No 500s, no 403s beyond the expected API Gateway propagation blip on the first call after key creation.
- **§1 scope EBUS access plan** — Scope expanded from "plan" to "execute." Inspected `InferenceAPIAuth-dev` structure (3 existing rows), updated `generate_api_key.py` to cover all current v5 roles (script had drifted — was missing `--motion_plus`, `--pet`, `--sliced_intruder_plus_with_vehicle`), minted a new `ebus-dev` row + API Gateway key ID `w9xqifo4o3` bound to usage plan `y36ojh`, verified end-to-end, and delivered the handoff credential block. Wrote two KB notes: [[ebus-partner-access]] as the operational record for this key, [[partner-api-credential-runbook]] as the generic recipe for next time.
- **Follow-up: script role-flag sync** — Committed the `generate_api_key.py` role-flag additions as a standalone PR (#57, commit `45353a2`) so anyone else's checkout gets the same flags. CI green across Test + Build + all three Terraform plans; squash-merged to `develop`. The argparse set is now aligned with `AcceptedRoles` in `check_api_key.py` and the comment calls out the sync invariant.

## Closed Sub-items

*Granular `[x]` checklist items closed today, swept from `mark-todos.md` on 2026-04-27. Date attribution from the line itself or section context (§3 plan-approval day).*

**§3 — New Lambda — AutoPatrol stale-schedule cleanup:**

- [x] §0 KB consolidation — 10 notes created/updated *(plan-approval day; KB consolidation completed when the plan was filed)*

**§3 misc:** the §3 plan was approved 2026-04-17; design synthesis [[2026-04-17_stale-schedule-cleanup-design]] landed today.

## Closed Workstreams

*(populated by /daily-wrap if any whole §N closed today)*

## Notes / Learnings

*(freeform — populated at EOD or progressively through the day)*

## Related

- [[mark-todos]] — scope picked from
- [[skill-daily-scope]], [[skill-daily-wrap]]
