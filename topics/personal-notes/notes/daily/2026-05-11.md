---
title: "Daily: 2026-05-11"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [llm-shop]
workstreams: ["§24"]
created: 2026-05-11
updated: 2026-05-11
author: kb-bot
---

# Daily: 2026-05-11

## Closed Line Items

- **§24 LLM-shop Phase 2H — overnight batch plumbing shipped.** Built `kb-batch-submit` / `kb-batch-pull` / `kb-batch-status` on the laptop and `kb-batch-runner.py` + `llm-shop-kb-batch@.service` on the box, plus a load-bearing linker fix (title-based anchor matching, hyphen-aware boundaries, skip-inside-wikilinks-and-code). End-to-end smoke run (`2026-05-11T1331Z-video-processing-001`, ollama backend, 2 URLs decord+vidgear, ~23 min wallclock) merged cleanly to `topics/video-processing/_research-inbox/` with reading-list lines flipped and manifest archived. Commits `0eec65c` (plumbing) + `e62252f` (catch-up source-control of the rest of the `~/llm-shop/` mirror). Build log appended to [[2026-05-07_overnight-batch-pattern]]; quality-tuning + first real overnight run carry into §24 open work.
- **§28 Billing R1 — admin↔emit reconciliation dashboard design landed.** Drafted [[2026-05-11_billing-reconciliation-dashboard-design]] specifying the daily reconciliation signal: NRQL+admin-DB data sources (Snowflake deferred to R2), `expected_pairs - observed_pairs` query shape, 7 edge cases pinned (site-level fallback, paused-Immix, act_a collapse, etc.), 3-phase surface plan (local Tier-1 → standalone billing dashboard → sales-dashboard integration), 5% → 1% threshold ramp, and 5 open impl questions for the follow-up PR. Cross-linked from billing/_todos R1, the post-mortem §3, and mark-todos §28. Closes the post-mortem's headline action item on the design side; implementation is the next loop per the [[2026-05-11_billing-and-followups-handoff]].
- **§28 Billing C2 — Snowflake DDL request filed as ENG-242.** Asked the data team for authoritative DDL on the billing table(s) downstream of `event_queue_analytics.fifo`, plus consumer-side filter logic for `event_type` / `act_a` / `admin_camera_id=null` / `_MISCONFIGURED_FALLBACK_PRODUCT`. Labeled `billing` / `snowflake` / `data-team-request`, linked to ENG-235 + ENG-200, unassigned (triage routes). Once landed: unblocks C5 + T2 + T3 + T4 + the R2 (Snowflake) half of the reconciliation dashboard. billing/_todos C2 updated with the ticket key and downstream-unblock map.
- **Pre-impl research priority reorder.** Closed handoff item #3: wrote [[2026-05-11_pre-impl-research-priority-reorder]] reranking the session-2026-05-11 fleet pre-impl list against billing as the new top-priority class. New ordering — top 3 are: (1) R1 impl PR [§28], (2) T1 spot-check of 5-10 silent containers [§28], (3) confirm `connector validate` subcommand exists [§5, already on the list]. Items 4-8 are fleet items shifted down a tier. ENG-242 / S1 / S2 / T1 final design are in a watch bucket (blocked on external response). Re-eval triggers: ENG-242 lands, PR #1688 merges, admin-team ADR responds, or 2026-05-25 (whichever first). Top-2 promoted to mark-todos §28 as sub-items.
- **§5 + §6 — handoffs #5 and #6 closed: reading-list additions across fleet and software-arch + enforcement-as-proposal-scorer spec.** Added 10 fleet reading-list items (K8s Jobs, API GW→Lambda→K8s — new section, Pydantic/Schema Governance — new section, Coordinator/Lease Mechanisms — new section, NATS-as-blob annotation, Spot economics, PyAV GIL) + 4 software-arch items (Connascence moved from fleet, tach-vs-import-linter benchmark, Operational Readiness & ODD — new section, Migration-Safety Patterns — new section incl. Strangler Fig + feature-flag migration). Then wrote [[2026-05-11_enforcement-as-proposal-scorer]] — handoff #6 spec defining how the architecture-enforcement sketch produces a quantitative Migration Risk input to the fleet rubric. Per-proposal target import-linter contracts sketched (A low, B high, C moderate, D highest, E moderate). Violation-count → bracket mapping (0-25→10, 600+→0) calibrated against existing rubric cells. Billing-emit-site fitness functions defined as proposal-agnostic separate contracts (centralization + idempotency-reach), with note that pure import-linter can't fully express idempotency check so handoff #7 needs a pytest sibling. Dual-use justification documented: pre-PoC scorer + post-PoC enforcer means investment pays off regardless of which proposal wins.

(Prior wrap line from earlier loop:)
- **§5 Fleet Architecture — handoff #4 closed: rubric extended with Monitoring & Alarms + Billing & Reconciliation dimensions, all 5 proposals rescored.** Wrote [[2026-05-11_rubric-monitoring-billing-dimensions]]. Weights rebalanced: IS 35→30, Cost 20→17.5, FI 15→12.5; M&A 5% (NEW), B&R 5% (NEW); OS/MR/FQ unchanged. New per-proposal scores on the 2 new dims after per-proposal rationale: A (M&A 7, B&R 9 — best B&R, internal-split preserves emit), B (6, 4 — 4-hop emission risk class), C (7, 8 — clean per-worker emission), D (4, 4 — heaviest monitoring + most fragmented emit), E (7, 8 — sidecar+core split scoreable). Recomputed composites: E 7.775 (lead narrows from 0.45 → 0.375), C 7.40 (unchanged — most rubric-robust), B 6.875 (−0.375), D 6.40 (−0.45), A 4.90 (+0.45 — biggest gainer from preserving emit). PoC-1=E PoC-2=C recommendation preserved; invalidation criteria sharpened (FDMD<40% threshold for E→C flip now a 0.1pt margin instead of 0.4pt). Baseline (today's monolith) rebaselined 3.20 → 3.60 on 8-dim view — still well below every in-play proposal.

(Prior wrap line from earlier loop:)
- **§28 Billing — NF2 fully deployed Tier-1 on Firebat, daily timer live.** Final loop wrapping the billing-reconciliation Tier-1 workstream: wrote `local_network_scripts/phase-16-billing-reconcile.sh` as the idempotent re-runnable deploy script. It attaches an inline IAM policy (`billing-reconcile-readonly`) to `dashboard-check-rolesanywhere` granting SM read on `prod/actuate/postgres-*`, installs uv + Python 3.13 on Firebat, clones sales-dashboard + actuate_bi, uv-syncs with CodeArtifact bypass, bootstraps .env from laptop's prod-SSO (patching `AWS_PROFILE=dashboard-check` on the way over to win the wrapper's env-merge), deploys the wrapper script + systemd .service/.timer (with ExecStart re-pointed to ~/bin/), appends 3 signals to `~/.claude/skills/dashboard-check/config/signals.json`, daemon-reloads + enables timer, and manually fires once to validate. Verification: `exit_status=ok`, `reconciliation.balanced=true`, `production_missing_subscription.cameras=2024` — signal is RED-by-design (value-add demo). Next fire: Tue 2026-05-12 11:00 UTC (04:00 PT, post-SPRD-swap by 3h). One fix during deploy: `.env` had `AWS_PROFILE=prod` which the wrapper overlaid on top of systemd's Environment; phase script now sed-patches to `dashboard-check` on the scp path. mark-todos §28 R1 sub-item flipped to [x], billing/_todos NF2 marked DEPLOYED with verification commands.

(Prior wrap line from earlier loop:)
- **§28 Billing — actuate_bi repo cloned + inventoried, ENG-242 closed Done, NF2 wrapper shipped.** Second loop on the sales-dashboard discovery: cloned `aegissystems/actuate_bi` (`/home/mork/work/actuate_bi`) — has the authoritative Snowflake-pipeline DDL in `tf/*.tf` (the `sql/snowflake/*.sql` files drift in spots). New KB entity [[actuate-bi-repo]] documents the deploy model (Terraform, manual apply), 7 scheduled tasks (SPRD swap at 01:00 ET, clip at 09:15 ET), and three operational findings: SPRD swap non-transactional with no `IF EXISTS` guard, `Load Top Parents` race at 01:01 ET, SQL/TF drift undetected by CI (subscription.sql missing `special_pricing` BOOLEAN). [[snowflake-billing-tables]] corrected — `usage_products` is at `raw.ordway`, not `gold.billing`; SPRD column list expanded to 24 columns. ENG-242 commented with findings, transitioned to **Done** — data team off the hook. Wrapper script for NF2 shipped at `/home/mork/work/local_network_scripts/files/billing-reconcile-check.py` — subprocess-invokes `reconcile_cameras.py` inside sales-dashboard venv, parses SUMMARY+FULL RECONCILIATION blocks via regex, emits JSON to `~/.local/state/minipc-tasks/billing/reconciliation-YYYY-MM-DD.json`, refuses to run without `SNOWFLAKE_PASSWORD` (lockout discipline), `--dry-run` validated. Source-of-truth `reconcile_cameras.py` not modified — reference from outside, library extraction tracked as NF6 if a second consumer surfaces. _todos updated: NF1 done, NF2 wrapper-shipped, NF3 reframed as "system-value-add demo not our scope to fix," NF6/NF7/NF8 added.

(Prior wrap line from earlier loop:)
- **§28 Billing — sales-dashboard repo discovery, ENG-242 substantially answered.** User surfaced `aegissystems/sales-dashboard` as the home for Snowflake-side billing infrastructure. Cloned to `/home/mork/work/sales-dashboard`, inventoried the snowflake.py client (2201 lines) + reconcile_cameras.py (754 lines) + CLAUDE.md/README. Findings: (a) `gold.billing.{usage_monthly, site_product_run_day, clip_received, top_parent}` + `raw.aws.analytics_event` are the canonical tables; (b) two silent-drop classes — `INNER JOIN raw.ordway.subscription` and `INNER JOIN top_parent + camera_flags_per_day` — explain Cohort F6/F5 exactly; (c) 3h billing threshold lives only in `usage_monthly` via `HAVING sum(quantity) >= 3`; (d) `reports@actuate.ai` Snowflake user via `prod/actuate/sales-dashboard` secret; (e) ingest is daily full-swap at 01:00 EST, NOT Snowpipe; (f) `reconcile_cameras.py` IS the R1 reconciliation implementation we just designed — 1-2d to promote to a Tier-1 signal vs 1wk from scratch. Three new KB notes written: [[snowflake-billing-tables]] (closes C5), [[sales-dashboard-repo]] (closes C6 — handoff item #8 done by side-effect), [[2026-05-11_eng-242-substantially-answered]] (concept). billing/_todos C2/C5/C6 statuses flipped, NF1-NF5 follow-ups added under new Category 7. Commented on ENG-242 with findings + narrowed ask ("is `actuate_bi/sql/snowflake/` the authoritative DDL source?"); left open pending data-team confirmation. Also revealed a Feb 2026 baseline of 444 production cameras across 25 accounts running billable products but unbilled (Active Watch Security 132, Aggregate Industries 102, Eagle Eye Networks 26, CAP Security 39, Alarm Watch 46, Bandit Systems 15) — direct revenue leak, NF3 follow-up. And 1,169 trial-pilot cameras (Securitas Australia trial: 738 cams @ 441 avg hrs) as conversion candidates, NF4.

## Closed Sub-items

_(none — work landed as a new phase row in §24, not sub-bullets)_

## Summary

_(to be filled by `/daily-wrap`)_
