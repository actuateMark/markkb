---
title: "Daily: 2026-05-11"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [llm-shop]
workstreams: ["§24"]
created: 2026-05-11
updated: 2026-05-11
author: kb-bot
---

# Daily: 2026-05-11

## Closed Line Items

- **§24 LLM-shop Phase 2H — overnight batch plumbing shipped.** Built `kb-batch-submit` / `kb-batch-pull` / `kb-batch-status` on the laptop and `kb-batch-runner.py` + `llm-shop-kb-batch@.service` on the box, plus a load-bearing linker fix (title-based anchor matching, hyphen-aware boundaries, skip-inside-wikilinks-and-code). End-to-end smoke run (`2026-05-11T1331Z-video-processing-001`, ollama backend, 2 URLs decord+vidgear, ~23 min wallclock) merged cleanly to `topics/video-processing/_research-inbox/`. Commits `0eec65c` + `e62252f`. Build log appended to [[2026-05-07_overnight-batch-pattern]].

- **§28 Billing — handoff #1 R1 admin↔emit reconciliation dashboard design.** [[2026-05-11_billing-reconciliation-dashboard-design]] — NRQL+admin-DB data sources, `expected_pairs − observed_pairs` query shape, 7 edge cases pinned, 3-phase surface plan, 5%→1% threshold ramp, 5 open impl questions. Closed the post-mortem's headline action item on design side.

- **§28 Billing — handoff #2 ENG-242 filed + closed Done same day.** Filed asking data team for Snowflake DDL + filter logic. Within hours, user surfaced [[sales-dashboard-repo]] as substantial answer; cloned + inventoried `clients/snowflake.py` (2201L) + `scripts/reconcile_cameras.py` (754L) + CLAUDE.md/README. Then cloned [[actuate-bi-repo]] (the actual pipeline DDL home — `tf/*.tf` authoritative, `sql/snowflake/*.sql` drifts). [[snowflake-billing-tables]] entity note captures full table inventory, two silent-drop classes (`INNER JOIN raw.ordway.subscription` + `INNER JOIN top_parent + camera_flags_per_day`), daily 01:00-ET full-swap mechanism (not Snowpipe). [[2026-05-11_eng-242-substantially-answered]] concept maps original ENG-242 asks → what we found. ENG-242 commented with findings, transitioned **Done**. Three KB entity notes + 1 concept note all cross-linked.

- **§28 Billing — handoff #3 pre-impl research priority reorder.** [[2026-05-11_pre-impl-research-priority-reorder]] reranks the session-2026-05-11 fleet pre-impl list against billing's new top-priority class. Top 3: (1) R1 impl PR, (2) T1 spot-check of 5-10 silent containers, (3) confirm `connector validate` subcommand exists. Items 4-8 are fleet items shifted down a tier. Watch bucket (blocked): ENG-242, S1, S2, T1 final design. Re-eval triggers documented (ENG-242 lands, PR #1688 merges, admin-team ADR responds, or 2026-05-25).

- **§28 Billing — NF2 fully deployed Tier-1 on Firebat (supersedes from-scratch R1 impl).** Wrapper `/home/mork/work/local_network_scripts/files/billing-reconcile-check.py` subprocess-invokes `sales-dashboard/scripts/reconcile_cameras.py` (referenced from outside — not modified), parses SUMMARY + FULL RECONCILIATION blocks via regex (25/25 fields), emits JSON to `~/.local/state/minipc-tasks/billing/reconciliation-YYYY-MM-DD.json`. systemd timer enabled on Firebat — daily 04:00 PT post-SPRD-swap. 3 signals appended to `~/.claude/skills/dashboard-check/config/signals.json` (`billing_production_unbilled_cams`, `billing_reconcile_residual`, `billing_reconcile_freshness`), all `enabled=true`. Deploy codified at [[phase-16-billing-reconcile.sh|local_network_scripts/phase-16-billing-reconcile.sh]] (idempotent, re-runnable). IAM: inline policy `billing-reconcile-readonly` on `dashboard-check-rolesanywhere`. **First real run 2026-05-11**: `exit_status=ok`, `reconciliation.balanced=true`, `production_missing_subscription.cameras=2024` (RED — up 150% from Feb 2026's 803; value-add demo working). LNS commit `6165a24`. State doc: [[2026-05-11_nf2-deployment-state]].

- **§5 Fleet — handoff #4 rubric extension (M&A + B&R dimensions).** [[2026-05-11_rubric-monitoring-billing-dimensions]] — weights rebalanced (IS 35→30, Cost 20→17.5, FI 15→12.5; +5pp each for Monitoring & Alarms + Billing & Reconciliation; OS/MR/FQ unchanged). All 5 proposals rescored on the 2 new dims with explicit rationale (A 7/9 best on B&R; B 6/4 worst on emit-multiplicity; C 7/8; D 4/4; E 7/8). Recomputed composites: E 7.775 (lead narrows 0.45→0.375), C 7.40 unchanged (most rubric-robust), B 6.875 (−0.375), D 6.40 (−0.45), A 4.90 (+0.45). PoC-1=E / PoC-2=C recommendation preserved; FDMD<40% E→C flip threshold sharpened from 0.4pt → 0.1pt margin.

- **§5 + §6 — handoff #5 reading-list additions across fleet + software-arch.** Fleet: +10 items + 3 new sections (API-GW → Lambda → K8s; Contract & Schema Governance; Coordinator / Lease Mechanisms). Software-arch: +4 items + 2 new sections (Operational Readiness & ODD; Migration-Safety Patterns). Connascence moved from fleet → software-arch.

- **§6 — handoff #6 enforcement-as-proposal-scorer spec.** [[2026-05-11_enforcement-as-proposal-scorer]] — per-proposal target import-linter contracts (A low ~10-50, B high ~200-500+, C moderate ~50-150, D highest ~400-1000+, E moderate ~50-150). Violation-count → Migration-Risk bracket mapping (0-25→10, 600+→0). Billing-emit-site fitness functions defined separately (centralization + idempotency-reach; needs pytest sibling for AST checks). Dual-use: pre-PoC scorer + post-PoC enforcer.

- **Handoff doc updates.** [[2026-05-11_billing-and-followups-handoff]] status table now marks 7-of-8 items closed; only #7 (enforcement collector code) remains.

- **Git: KB commit `3784ba7`** (16 files, +3140/-471 — billing arc, fleet rubric, software-arch spec, handoff closures). **LNS commit `6165a24`** (5 files, +864 — NF2 deploy artifacts: phase-16, wrapper, systemd units, IAM policy). Neither pushed; awaits user discretion.

- **§28 Billing NF10 — wrapper sink filename fix.** Surfaced by NF9 replay: `--month X` runs all clobbered `reconciliation-$(date +%F).json` (keyed by today, not analyzed month). Fixed wrapper to write `reconciliation-YYYY-MM.json` (analyzed month). Updated signals.json commands on Firebat from `$(date +%F)` → `$(date +%Y-%m)`. Stale sink cleaned. Re-verified all 3 signals read correctly. Wrapper change committed to local_network_scripts (3-line diff to `_emit()`).

- **§28 Billing NF9 — historical replay validates "would have caught Cohort F" claim.** Ran [[2026-05-11_nf2-deployment-state|NF2]] wrapper on Firebat against March + April 2026 via `--month`. Trend: Feb 803 → Mar 2,353 → Apr 3,152 → May (partial) 2,024 production cameras missing Ordway subscription. **Signal would have fired RED on March 2026 data**, ~2 months before Cohort F's manual discovery on 2026-05-04. Also confirms connector-side emit reliability monotonically improved (88% → 95.9%) — PR-#1675→#1688 fixes landed and held. Trend table in [[2026-05-11_nf2-deployment-state]] §"Historical replay (NF9)." Side-finding: wrapper sink filename `reconciliation-$(date +%F).json` uses today's date not analyzed-month, so historical replays clobber. Tracked as NF10.

## Closed Sub-items

_(none — work landed as a new phase row in §24, not sub-bullets)_

## Summary

_(to be filled by `/daily-wrap`)_
