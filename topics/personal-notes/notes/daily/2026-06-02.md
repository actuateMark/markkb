---
title: "Daily: 2026-06-02"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [vms-connector, video-processing, actuate-libraries, profiling-and-performance]
workstreams: ["§15"]
created: 2026-06-02
updated: 2026-06-03
author: kb-bot
---

# Daily: 2026-06-02

## Summary

A single-focus day: the **PyAV-17 / FFmpeg-8 / OpenCV-4.13 corner-case deep-plan (#1703, ENG-136)** — the explicit headline pick, push held until every corner case has a mitigation/test plan. Substantial progress but not finished (8 items remain). Also ran the post-deploy verification of #1713 (GREEN — captured in [[2026-06-01]]) and the two morning rituals (wrap 06-01, scope 06-02). The other three scoped picks (verifier ask, onboarder #14/#15/#16, brain-in-jar #359) weren't started. All four carry to 06-03. Full working state: [[2026-06-02_handoff-pyav17-corner-case-plan]].

## Closed Line Items

*Nothing fully closed today — the day was one large in-progress workstream (see Notes). Items carried forward.*

## Closed Sub-items

*None swept.*

## Closed Workstreams

*(none)*

## Notes / Learnings

**PyAV-17 deep-plan — major progress (in-progress, carried to 06-03).** Handoff: [[2026-06-02_handoff-pyav17-corner-case-plan]]. Highlights:

- **Risk re-assessment against actual code** (via `connector-pipeline-expert`): the two scariest risks shrank. **C2 (reconnect storm) DEBUNKED** — no frame-drought watchdog exists, so suppressed-corrupt-frames produce a quieter pipeline, not reconnects; the headline benefit isn't self-defeating. **C1 (AVDISCARD starvation thrash) is real but benign** — log-only, ~10s slow cycle, scoped to software-decoded dense-keyframe HEVC. **Stale-preview** is real and unguarded.
- **Custom FFmpeg build bumped to n8.1.1** (D1 = keep custom build, locked). The issue under-counted: FFmpeg is pinned in **3 source places**, not 1. Edited all three (`build_ffmpeg.sh`, `arm_dockerfile.gpu`, `x86_dockerfile.gpu`) + folded in the missing **`--enable-gnutls`** on both GPU builds — closes the **§15a** GPU rtsps/https gap. **Edits UNCOMMITTED on `feature/pyav-17-bump-clean`.** PR #1714 had been missing its entire FFmpeg half (only bumped pins, never the dockerfiles).
- **Bug found that #1703 AND merged lib PR #358 both missed:** `CodecContext.close()` was removed in PyAV 16 (verified against the cached av-17.0.1 wheel). `actuate-pullers` still calls it at main `av_url_puller.py:841,940` — currently masked by a bare `except: pass` (silent-broken, not crashing). Fix = drop the calls; needs a new `[patch:actuate-pullers]` PR. (`av.AVError` is already handled robustly via a getattr shim — non-issue.)
- **Remaining (8):** nvdec close fix + C1 hysteresis guard + stale-preview guard (actuate-pullers), build validation (ARM-CPU + GPU), re-confirm MISS-685 signal on cust 41399, rebase #1714, promote, full synthesis.

**Sibling-session KB activity 06-02** (not my scope, noted for cross-ref): watchman frontend sketch + fleet-arch `watchman-phase0-fleet-fit` (§5), infrastructure `dynamodb-fit-assessment-ait-phase-10` + `dynamodb-mcp-tool`, `operational-health/2026-06-02_overnight-check`.

## Related

- [[mark-todos]] — scope picked from
- [[2026-06-02_handoff-pyav17-corner-case-plan]] — PyAV working state + resume checklist
- [[2026-05-26_pyav17-local-validation]]
- [[skill-daily-scope]], [[skill-daily-wrap]]
