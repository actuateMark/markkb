---
title: "Daily: 2026-05-27"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [actuate-integration-tools, actuate-libraries, engineering-process, personal-notes, autopatrol, vms-connector, admin-api, billing, actuate-platform]
workstreams: ["§30", "§29", "§3", "§10"]
created: 2026-05-27
updated: 2026-05-28
author: kb-bot
---

# Daily: 2026-05-27

## Summary

Three concurrent sessions converged into a heavy ship-and-validate day. **Primary thread (#1703 PyAV-17 release train):** the §29 prod-admin custom-branch deploy mechanism saw its first prod use — cust 705 (alibi-vigilant) pinned to `featurepyav-17-bump-clean`, kubectl-confirmed running on the feature image, av17 booted clean (T2 runtime gap closed). Extended to a **3-site overnight soak** (705 alibi/rtsp + 573 avigilon + 1751 genetec) for integration-path coverage; revert + verdict seeded as 2026-05-28 Morning Follow-Ups (TTL won't auto-revert — cron unbuilt). Surfaced + corrected a recurring misunderstanding: **`rearchitecture` is a PROD fleet (not pre-prod) — there are two parallel prod fleets** (master legacy + rearchitecture); captured in `branch-conventions` + `feedback-rearch-is-a-prod-fleet` memory. Wrote `admin-api-auth` entity documenting the DRF-token path + dead ends. Three follow-up gaps logged as tasks: build `expire_custom_branches` cron (TTL auto-revert), add `container_name`/`deployment_id` filters to CustomerFilter (had to ask Mark for PKs manually), update PRs with overnight soak results. **PR #1713 → prod still blocked on REVIEW_REQUIRED.** **Sibling-session shipments:** brain-in-jar arc cleared to a durable handoff (5 lib commits unpushed, 91 new tests, Phase 4/5/6a/6b/7/11 shipped, Phases 8/12 blocked on Zack coordination — see [[2026-05-27_brain-in-jar-handoff]] + [[2026-05-27_zack-coordination-brain-in-jar]]); AP empty-metrics arc closed (PR #2451 admin-side fallback MERGED + PR #1712 connector-side WARN; all 3 affected sites remediated; NR MCP OAuth confirmed broken — use `nr_query.py` bypass).

## Closed Line Items

- **#1703 dry-run on cust 705 — fully validated.** First `deploy_branch` call in prod (CustomBranch id:1, event_id:2). Kubectl confirmed `actuate-nyc-alibi-vigilant-7df8477665-qf99v` running `arm_connector_rearch:featurepyav-17-bump-clean`, started 17:47:32Z (matches event timestamp). NR: container booted clean, pullers reconnected, h264 decode running, **zero av exceptions**. Closes the T2-runtime gap that local validation couldn't produce (no frame flow in T2-lite). Token saved at `~/.config/actuate/admin_token` (chmod 600) for reuse. Full sequence: [[2026-05-26_pyav17-local-validation]] + [[admin-api-auth]].
- **3-site overnight PyAV-17 soak deployed.** Added 573 avigilon + 1751 genetec to the soak alongside 705. All three kubectl-confirmed on the feature image; revert seeded for 2026-05-28 morning. h264-only — HEVC member (AmeriGas clone) deferred to focused session per the live-camera-contention risk surfaced.
- **Branch ↔ ECR ↔ deployment-fleet topology documented.** Discovered + captured: vms-connector has TWO parallel prod fleets — `master`→`arm_connector:latest` (legacy) and `rearchitecture`→`arm_connector_rearch:latest` (new arch). `stage`/`rearch-dev`/custom branches use the rearch ECR repo with distinct tags. Merging to `rearchitecture` is a PROD deploy (corrects the recurring "rearch = pre-prod" mistake that bit the standup twice). New section in [[branch-conventions]] + `feedback-rearch-is-a-prod-fleet` memory.
- **Prod admin API programmatic-auth reference written.** [[admin-api-auth]] entity captures: DRF token via `/authtoken/tokenproxy/` (one-per-user, reuse from list), `TokenAuthenticationStrict` accepts standard `Authorization: Token`, ECS Exec is disabled on `prod-camera-admin`, no token in Secrets Manager, customer route is `/api/customer/` (singular), `branch_status` gives real deployment state, kubectl recipe to verify the running image definitively.
- **PR #91 hand-review + live-verify on dev** *(carry-over from 2026-05-22, decision still held)* — reviewed Jacob's split-intruder hybrid + v5 verification scaffolding; 14/14 live API assertions passed on dev. Prod-merge decision deferred again pending Mark.
- **Standup consolidated for Friday + yesterday.** Wrote a topic-mode 3-question standup spanning 2026-05-22 + 2026-05-26. Iterated three times as the rearch=prod-fleet correction propagated — final version says "shipped to the rearchitecture prod fleet" honestly.
- **Brain-in-jar arc — session-cleared via handoff** *(sibling session)*. 5 library commits sitting on `feat/idp-serializer-brain-in-jar-phase-4` (unpushed); AIT phases 4/5/6a/6b/7/11 shipped, Phases 3/8/9/10/12 sketched. Full handoff at [[2026-05-27_brain-in-jar-handoff]]; Zack-bound coordination doc at [[2026-05-27_zack-coordination-brain-in-jar]] (Plays A/B/C asks).
- **AP empty-metrics arc closed end-to-end** *(sibling session)*. actuate_admin [PR #2451](https://github.com/aegissystems/actuate_admin/pull/2451) **MERGED** (admin-side fallback to `stream.stream_metrics.all()` + WARN). All 3 affected sites verified remediated in prod (46767 / 37837 / 41070); regen required admin-UI saves (gap → backlog: "AP schedule settings regen-from-here capability"). Dashboard signals added. KB: [[autopatrol-aws-objects]]. Tooling finding: NR MCP OAuth broken; `~/.claude/lib/nr_query.py` bypass is the path.

## Closed Sub-items

## Closed Workstreams

## Notes / Learnings

### Brain-in-jar arc — session-cleared with handoff + Zack-bound coordination

Spent the session writing two cross-referenced KB notes to fully clear context out of conversation history and into durable docs:

- [[2026-05-27_brain-in-jar-handoff]] — personal handoff. Captures repo state across `actuate-libraries` (5 commits unpushed on `feat/idp-serializer-brain-in-jar-phase-4`) + `actuate-integration-tools` (local-only, all phases on `main`), the full phase completion map, test totals across 6 packages (~91 new tests), pending action items ranked P0/P1/P2, and a resumption checklist for the next session.
- [[2026-05-27_zack-coordination-brain-in-jar]] — Zack-bound coordination doc. Written copy-pasteable for Slack / email / PR body. Walks Zack through what landed across libs, the 6-play integration plan with **specific asks for Plays A (validator adopts our factories), B (lift `MockDaoManager` to `actuate-daos/testing/`), C (lift `MockImageCache` similarly)**, and open questions (library-home for actuate-validator, score-based regression model, naming convention).

**Key state at handoff:**

- 5 library commits sit on `feat/idp-serializer-brain-in-jar-phase-4`, not yet pushed: `[patch:actuate-pipeline-objects]` + `[minor:actuate-instrumentation]` + `[patch:actuate-pipeline]` (Phase 4) + `[minor:actuate-pipeline-objects]` (Phase 11) + `[minor:actuate-alarm-senders]` (Phase 7) + `[minor:actuate-pullers]` (Phase 5).
- AIT path-pins `actuate-pipeline-objects`, `actuate-instrumentation`, `actuate-pipeline` to the local libs checkout via `[tool.uv.sources]`. Drop to version-pins once libs publish.
- Brain-in-jar arc: 6 phases shipped (4, 5, 6a, 6b, 7, 11), 4 sketched (3, 8, 9, 10, 12). Phase 8 + 12 blocked on Zack coordination; Phases 9 + 10 independent.

**Decisions captured (just locked-in this session):**

- Perf-discipline rule: replay must be **1:1 or off-by-default**. No polling threads at steady state; env-var-gated wrappers short-circuit at init. Phase 9's polling watcher was dropped entirely in favor of `faulthandler.register` + `SIGUSR1` + Python exception handlers + `atexit`.
- QA workflow gap captured as Phase 12 (`ait sweep`) — given a video + expected alert, sweep parameters to find a config that produces it. Blocked on Plays A+B.
- Master synthesis [[2026-05-22_actuate-testing-toolkit-overview]] is the single document that maps all five tools and answers "which one do I reach for."

**Next session prep:** open [[2026-05-27_brain-in-jar-handoff]] first. Repo states + test-smoke commands are in the resumption checklist.

### AP empty-metrics — end-to-end prod verification + close-out (coworker triage arc)

Closed out the multi-day arc that started 2026-05-21 (coworker NR query → `connector-46767-autopatrol-1102` emitting `misconfigured` billing fallback despite products being configured). Both fixes are now live and the three affected sites are verified remediated in prod.

**Final state:**
- **actuate_admin PR [#2451](https://github.com/aegissystems/actuate_admin/pull/2451)** (commit `0d8f1e5a`) **MERGED** (by Tatiana, base resolved to `staging`) — confirmed present in `main` + `develop` + `staging`. `SettingsGenerator.list_metrics_by_schedule` now falls back to `stream.stream_metrics.all()` + WARN (`ap_metrics_fallback_all`) when the AP schedule_product_metrics intersection is empty, instead of writing empty `metrics: {}` to S3.
- **vms-connector PR #1712** (merged earlier to stage) — the connector-side `empty_metrics_dict` WARN guard. Double-layered detection.
- **All 3 sites remediated, all State A** (intersection-empty, fixable by fallback — none needed a stream-side data fix):
  - 46767 — self-recovered 2026-05-21
  - 37837 — regen 18:12 UTC; 19:00 cron tick clean: **0 fallback, 25 real `site_product_ended`** (`Intruder +`, `Crowd`)
  - 41070 — needed **two** regen attempts (first save silently didn't reach admin — zero `prod_camera_admin` log activity; second at 18:26 landed); 19:00 tick clean: **0 fallback, 24 real product events**
- Regeneration required **admin-UI saves** — the code fix only changes future generation, it doesn't rewrite existing S3 `settings.json`. This is the gap captured as a backlog item (below).

**Tooling finding — NR MCP OAuth is broken, use the bypass wrapper:** the `mcp__newrelic__*` MCP (an `mcp-remote` proxy to `https://mcp.newrelic.com/mcp/`) hangs on every query because its OAuth callback to `localhost:43663` fails ("problem with that site" = the ephemeral listener times out before browser login completes). Root state found in `~/.mcp-auth/mcp-remote-0.1.36/`: expired token (Feb 27) + a half-finished re-auth (code_verifier written today, no `tokens.json`), plus **6 stale `mcp-remote` proxy procs** piled up from old sessions. **Resolution: don't fight the OAuth — use the documented MCP-bypass `~/.claude/lib/nr_query.py`** (NerdGraph REST + User API key, [[2026-04-27_headless-mcp-bypass]]). All verification this session ran through it, zero auth friction. Note: leading-wildcard `LIKE '%...%'` over multi-day full-`Log` scans times out (NRDB:1109) — scope to `container_name` + short windows per the standing NR query rules.

**KB written:**
- [[autopatrol-aws-objects]] — the AP object set: 2 S3 buckets (`autopatrol-patrols` final payload, `autopatrol-queue-archive` inbound message) + 3 DDB tables (`autopatrol_results` index, `autopatrol_alerts`, `autopatrol_chm_issues`). Key cross-service insight: **connector writes the 3 DDB tables, AP server writes the 2 S3 buckets.** `autopatrol_chm_issues` is written by the VCH alert sender (any VCH/CHM run, not AP-only) despite the prefix.
- Backlog item **"AP schedule settings regen-from-here capability"** in [[autopatrol-deferred-backlog]] — there's no self-serve way to trigger a settings-only regen (`deploy_schedule_settings(call_deployer=False)`); today needed admin-UI saves. Proposed a dedicated `POST /api/autopatrol_schedule/{id}/redeploy_settings/` action + an `admin_write.py` bypass helper. Also note: 37837 + 41070 were already flagged in this same backlog under "6 autopatrol pods looping empty queue" (2026-05-19) — empty-metrics was the root cause.

**Dashboard signals added** (`~/.claude/skills/dashboard-check/config/signals.json`): `ap_empty_metrics_warn_6h` (connector-side, `empty_metrics_dict` OR `no products configured`) + `ap_metrics_admin_fallback_24h` (admin-side `ap_metrics_fallback_all`). Both should sit at 0 now; alarm on any new regression.

## Related

- [[2026-05-27_brain-in-jar-handoff]] — full handoff doc
- [[2026-05-27_zack-coordination-brain-in-jar]] — Zack-bound coordination
- [[2026-05-22_actuate-testing-toolkit-overview]] — master synthesis
- [[2026-05-21_ait-validator-integration-plan]] — gating doc for further AIT dev
- [[mark-todos]]
- [[skill-daily-scope]], [[skill-daily-wrap]]
