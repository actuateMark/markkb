---
title: "Daily: 2026-04-23"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [autopatrol, vms-connector, software-architecture, operational-health, infrastructure, personal-laptop, repo-backlog, engineering-process]
workstreams: ["§1", "§3", "§6", "§9", "§9-old (superseded)", "§10", "§11"]
created: 2026-04-23
updated: 2026-04-27
author: kb-bot
---


# Daily: 2026-04-23

## Summary

All six Today's Scope items closed. **Morning**: S3 Tier3 cost dive root-caused to lifecycle-expiration (not replication) on 24h-auto-delete frame buckets + Standard→IA→DEEP_ARCHIVE transitions; overnight NR check surfaced a 4x OOM spike. **Afternoon**: OOM spike diagnosed as VPA memory-limit drift on ~2,000 connector pods from a 2026-02-09 commit silently removing the lead-agnostic memory floor — `connector_deployer#158` filed with two fix candidates; full 5-repo back-catalog (186 issues) curated into idempotent per-repo catalogs at `topics/repo-backlog/notes/concepts/*.md`; §3 cleanup-lambda flip handed off to sibling session; `actuate_admin#2235` triage closed as stale (fix already landed 2026-02-04). **Evening**: §6 software-arch sketches init bundle — first commit `6f80028` + uv migration + utcnow fix + CodeArtifact index persistence — and shipped sketch 1/5 (commit `263cd1a`): radon cyclomatic complexity collector across `vms-connector` (239 files / 1,370 functions / ~0.5s; mean 3.62, p95 13, max 121). §9 Operational Dashboard workstream added cross-cutting. **Late evening (post-wrap session)**: provisioned the Firebat minipc as a personal always-on Claude Code dev box end-to-end — clean reset of the old microk8s dev-node role, switched network stack to NetworkManager, joined Tailscale (`mork-firebat`), ported wifi creds with priority-based autoconnect, installed headless Obsidian (1.12.7 via `/config` AppImage extraction — shipped binary was from Oct 2022), joined Obsidian Sync, stood up `http://mork-firebat/` status dashboard (mDNS + Caddy + 30s systemd timer), wired persistent tmux via linger + user systemd. Final verify: 22 pass / 0 fail / 0 skip across reboot. Entire provisioning captured as a 12-phase scripts toolkit in `~/work/local_network_scripts/` reusable for future boxes. **Post-wrap addendum (23:00Z–00:15Z)**: §3 cleanup-Lambda saga. PR #7 merged (Immix HTTP 400 → "gone", not transient). Silent-PATCH-drop bug found during the first "cleanup disable" attempt at 22:09:57Z (strict-camelCase serializer silently dropped snake_case + `is_deleted` which isn't a serializer field at all). PR #8 fix: PATCH `scheduleStatus="Deleted"` (triggers existing deploy→undeploy chain) + `disabledBy`/`disabledAt` for audit + 3-field post-PATCH verify. Merged + deployed + 23:03:26Z synthetic invoke produced the **first correctly-landing Lambda cleanup disable** (admin_pk=235). 14-day roster sweep: 1 genuinely gone (235, cleaned up), 4 Immix-says-active (safety net anomaly-resets), 2 orphaned from admin (→ [connector_deployer#160](https://github.com/aegissystems/connector_deployer/issues/160) filed for deployer-side orphan scan). `daily-wrap` skill extended with Step 5.5 (carry-forward → KB handoff notes wikilinked from mark-todos) after a handoff was lost between sessions earlier today. **Archived**: §1 Inference API v5 (dev-tier complete via EBUS handoff 2026-04-17; prod tier lives on as a post-archive follow-up).

## Closed Line Items

- **Tier3 S3 cost dive** — identified lifecycle-expiration (not replication) as the Tier3 driver; primary source is 24h-auto-delete frame buckets (`aegis-all-frames-v2-*`, `all-frames-aegis-v2`); secondary is Standard→IA→DEEP_ARCHIVE transitions on actuate-2/6-month-storage. No Intelligent-Tiering; no cross-region replication configured. Full synthesis: [[2026-04-23_s3-tier3-cost-investigation]]; raw data: [[2026-04-23_s3-tier3-breakdown]]. Two small independent quick-wins surfaced: `aegis-all-frames-v2-sts` has a disabled lifecycle rule + accumulating StandardStorage; `actuate-envera-frame-0` writes StandardIA at PUT time with no matching lifecycle rule.
- **§3 Step E gate verification (partial)** — CloudWatch confirmed `lambda_immix_autopatrol_schedule_cleanup` fired at 2026-04-22T18:04:26Z for admin_pk=138 / schedule `ee1822f1-...` with `enabled=False dry_run=False records=1`. Counter hit threshold 8/8, then the Immix-confirmation anomaly guard correctly detected "schedule still active" and reset the bucket rather than proceeding toward disable. The gate path works end-to-end; the merge-legitimacy question is already closed per user (prod checked, no regression). Next concrete action is the stage flip of `CLEANUP_ENABLED=true`.
- **§9 Operational Dashboard — Phase 1a COMPLETE** — `/dashboard-check` skill scaffolded end-to-end at `/home/mork/.claude/skills/dashboard-check/`. Output at `/home/mork/Documents/worklog/dashboard/2026-04-23/index.html` (open in browser). 10 pytest tests pass including the canonical acceptance test that catches the 2026-04-23 silent-early-return pattern. Live smoke-run against us-west-2 onboarder produced GREEN (activity=70/h, invocations=11/h, ratio 6.4× — not anti-pattern). Surfaced + fixed a calibration bug on first run: postmortem's "Fetched N contracts" fingerprint didn't match real logs; switched to `get_sites HTTP`. Plan file: `/home/mork/.claude/plans/clever-tickling-swing.md`. Design sketch: `topics/operational-health/notes/syntheses/2026-04-23_dashboard-sketch.md`. Phase 1b pickup doc (READ FIRST tomorrow): `topics/operational-health/notes/concepts/2026-04-23_dashboard-phase-1b-pickup.md`.
- **Dashboard HTTP serving set up** — systemd user service `dashboard-server.service` (enabled, auto-starts at login) running `python3 -m http.server 8765 --bind 127.0.0.1 --directory ~/Documents/worklog/dashboard/`. Bookmark: **http://localhost:8765/** (redirects to today's snapshot via the index.html meta-refresh shim + `latest/` symlink). Persists across reboots.
- **§10 Portability workstream scoped** — new cross-cutting workstream to survive laptop loss: dotfiles + bootstrap.sh + secrets-refresh runbook + disaster-recovery test. Added to mark-todos §10; placeholder task #8 created. No action today.

### Afternoon session (post-clear, ~12:00–15:00Z)

- **OOM surge triage (4x baseline) — ROOT CAUSED** — plateau pattern ruled out single-trigger / node pressure / deploy event. Traced to VPA-managed pod memory limits drifting below working-set requirements on ~2,000 connector pods. Cohort breakdown: 1,956 pods at 384–426 MB, 840 at 480–652 MB; baseline correctly-sized connector is 1.6 GB. Connector-20628 was patched mid-day (384 MB → 1.6 GB limit); OOMs stopped immediately. Release-tracked to `connector_deployer` commit `a5de5db` (2026-02-09, "remove vpa patch") which removed the lead-agnostic minimum-memory floor for VPA; `9736971` + `4367a39` (2026-03-04) selectively restored it for `"Securitas Australia - Trial"` only. 73-day compounding drift. Full KB synthesis: [[2026-04-23_oom-surge-connector-limit-drift]]. Process rule extension: [[2026-04-23_release-acceptance-criteria]] §5 "Track config-surface drift over time" added — codifies that release validation must watch config invariants (VPA limits, S3 lifecycle, rate ceilings) over time, not just deploy-day behavior. Issue filed: [connector_deployer#158](https://github.com/aegissystems/connector_deployer/issues/158) with two fix candidates (raise `minAllowed: 200Mi → 1Gi` in `vpa_cronjob.py`, AND/OR restore lead-agnostic floor in `generate_vpa_patch()`).
- **actuate_admin#2235 security triage — CLOSED STALE** — "History endpoint exposes dev streams" filed 2026-02-03; fix landed next day (`cdcc9248` "Exclude dev streams from history" / EMISC-34); refactored 2026-03-13 (`7c96c09c` "Centralise stream access-control in StreamManager.with_access()"). Current state: both `get_queryset` and `get_history_queryset` in `api/serializers/metric/metric_view.py` delegate to `Stream.objects.with_access(user)`, which for non-superusers applies `.exclude(name__icontains="dev")` centrally. No live leak. Issue closed with commit-linked explanation.
- **§3 Step E flip decision — HANDED OFF** — user chose "promote to prod then flip if green" path. That scope is owned by the cleanup-lambda session; this session will not touch the lambda. Gating context (merge-legitimacy CLOSED, CloudWatch gate VERIFIED) recorded in [[2026-04-22_cleanup-lambda-bake-state]].
- **Sibling-session autopatrol_onboarder commits (informational)** — 4 commits landed today in `autopatrol_onboarder` from the cleanup-lambda session: `78f8852` hotfix to stop onboarder from bailing on healthcheck 404 (ties to the post-mortem), `93af9d0` idempotent counter increment on SQS retries (#5), `755a273` deploy workflow to fail on real errors not just `ResourceNotFound` (#6), `d05d960` cleanup-lambda treating Immix 400 as "gone" (not transient). Owned by that session's wrap; noted here because they directly shape §3 and §9 state this session referenced.
- **Dashboard UX expansion — SHIPPED** — compact grid (`<details>` + CSS grid, no more bloated description column), hero carousel (3 cards: status heat-grid, top regressions, signals summary; CSS-only radio switching, `<` `>` arrows + dropdown), rich drawer per signal (description, thresholds, query, data-source badge, last-observed, history count, KB rationale). `Evaluation` dataclass extended with `query`, `kb_link`, `source`, `would_have_caught`, `data_source`, `last_observed_at`, `freshness_hours`, `history`. Templates: `macros.j2`, `component.html.j2`, `index.html.j2`. CSS appended: `src-badge` variants, `hero-*` classes.
- **Dashboard backfill mini-framework — SHIPPED** — `~/.claude/skills/dashboard-check/backfill.py` with 4 adapters (NRQL, cw_metric, cw_insights, ce_daily), idempotent `(signal_id, minute-timestamp)` dedupe, `--dry-run`. 198 sink records seeded across 5 signals in 7d window (30d for cost). Pickup-doc deliverable #15a documents the pattern for Phase 1b to extend with test fixtures, registry, orchestrator (#15b).
- **Sink-fallback grid rendering — SHIPPED** — grid is now source-aware. Every enabled signal renders via best-source-at-render-time: live tempdir → `sink_recent` → `sink_stale` → `none`. Never lies about freshness. New optional signal field `freshness_threshold_hours` (overrides default `window_hours × 2`). 5 signals enabled (`fleet_oomkills_24h`, `streamid_guid_rejection_stage`, `fleet_error_top15`, `cost_s3_daily_total`, plus 2 live onboarder signals). Grid now renders 6 signals across 3 components. Architectural implication: future signals become additive — backfill once, flip enabled, it appears in grid with sink rendering; wiring a live collector later silently upgrades to `live` with zero UX change. Details in pickup doc #15-zero.
- **Repo back-catalog curated** — Today's Scope item closed. Ran full `/repo-scan --refresh`: **186 open issues** across 5 active repos (100 vms-connector, 49 actuate_admin, 33 actuate-libraries, 3 actuate-inference-api, 1 autopatrol-server; autopatrol_onboarder + camera-ui empty). Five per-repo catalogs written at `topics/repo-backlog/notes/concepts/*.md` with idempotent schema: hand-maintained `## Curated notes` above + sentinel-bounded `<!-- BEGIN-AUTO-REFRESH repo-scan -->` block (regenerated each run) below. Frontmatter carries full issue-number lists (`high_impact_issue_numbers`, `lhf_issue_numbers`, `stale_issue_numbers`, `full_issue_numbers`) so truncated items remain Base-queryable. Curation script permanent at `~/.claude/skills/repo-scan/curate.py`. Curated-notes starters hand-written for the 3 fat repos (`vms-connector.md`, `actuate_admin.md`, `actuate-libraries.md`) identifying active clusters (e.g. vms-connector's RTSP robustness suite 1648-1652, DDB throttling pair 1640/1642, 7-ticket healthcheck bug cluster) and cross-repo ties (libraries#240 ↔ connector#1648-1652 RTSP, libraries#237 ↔ admin#2177 EU). **Stale framing:** actuate_admin has 41 stale issues (>180d idle) framed as **codebase-scan follow-up candidates, never bulk-close** — 5-batch triage plan captured in that catalog. `/repo-scan` SKILL.md extended with Step 4b (per-repo refresh) + cadence notes; daily vs weekly review seeded for 2026-04-30. Dated scan: `topics/repo-backlog/notes/scans/2026-04-23_scan.md`. Summary updated: `topics/repo-backlog/_summary.md`.

### Evening session (post-clear, ~19:00Z)

- **§6 software-arch sketches — init bundle CLOSED.** Today's Scope item #2 (carry-forward from 2026-04-22). Initial commit `6f80028` on `main` of `/home/mork/work/software-arch-sketches/`. Resolved all four sub-tasks in one commit:
  - **Scaffold commit** landed; local-only, no remote created (plan to push once sketches produce signal worth sharing).
  - **utcnow fix** — all 4 `datetime.utcnow()` call-sites (`metrics/collector.py`, `enforcement/rules.py`, `debt/patrol.py`, `tooling/runners.py`) migrated to `datetime.now(timezone.utc)`. Verified with `python -W error::DeprecationWarning` on every entry point.
  - **uv migration** — Makefile install target switched from `pip install -e ".[dev]"` to `uv sync --all-extras`; all per-sketch targets use `uv run python`. Test + lint targets likewise.
  - **Index persistence resolved** — user-global uv config pins CodeArtifact as default, and user-shell `UV_INDEX` + `UV_INDEX_CODEARTIFACT_PASSWORD` env vars override any local config. Fix: local `uv.toml` declaring public PyPI as `default = true` and CodeArtifact as `explicit = true` (reachable via `[tool.uv.sources]` override for dev-version pulls); Makefile `install` runs with those env vars unset and `--no-config --default-index https://pypi.org/simple/` to bootstrap. `uv.lock` committed so subsequent `uv sync` (including from user's normal CA-default shell) uses the lockfile and never hits any index. Documented end-to-end in README "Pulling dev versions of actuate-libraries".
  - Plan note [[2026-04-17_local-sketches-plan]] §"Next concrete steps" still stands — first real sketch is metrics collector (radon cc across vms-connector). Tests 6/6 green.
- **§6 sketch 1/5 — metrics collector SHIPPED.** Commit `263cd1a`. `software_arch_sketches.metrics.collector` walks `SKETCH_INPUT_REPO` for `.py`, runs radon `cc_visit` per file, emits per-file + summary envelope to `data/metrics.json`. Verified against `vms-connector`: 239 files, 1,370 functions, mean 3.62, median 2, p95 13, max 121 (top offender `AnalyticsSiteManager._log_memory_breakdown`); rank split A/B/C/D/E/F = 1146/128/67/22/4/3; 0 parse failures; ~0.5s runtime. Spotted + fixed a statistics-vs-display bug during implementation: first draft computed summary over the top-10-per-file display subset, biasing mean high (4.35 vs real 3.62). Final shape separates `_all_complexities` (private, for stats) from `top_issues` (capped, for display). ~130 LoC total. Smoke test rewritten to exercise the real collector on an empty fake repo. KB: [[2026-04-23_sketch-findings-metrics]].

### Late-evening session (post-wrap, ~20:00Z–23:30Z)

- **Firebat minipc — always-on Claude dev box SHIPPED end-to-end.** New §11 in mark-todos; [[2026-04-23_firebat-minipc-as-claude-dev-box]] is the architecture synthesis. Starting state: slow-booting Firebat_Computer AM02 with no HDMI output, unknown admin creds, preconfigured with team microk8s + a dormant WireGuard tunnel to an Oracle Cloud peer. End state: clean-reset Ubuntu 24.04 box reachable as `mork@mork-firebat` over Tailscale from anywhere (phone cellular included), running headless Obsidian joined to Obsidian Sync, persistent tmux session, working `claude -p` with auth carried over from laptop (verified end-to-end via `bin/claude-run-skill.sh recap` producing 2.3 KB of real `/recap` output off the synced vault), `http://mork-firebat/` status dashboard with `/obsidian`, `/dashboard`, `/logs` reverse-proxies + an auto-regenerated status page. All 12 provisioning phases packaged as reusable idempotent scripts in `~/work/local_network_scripts/` — point `TARGET=user@host` at any future Ubuntu box and re-run. Reboot test: 22 pass / 0 fail / 0 skip — every capability self-recovered autonomously in 66 seconds (wifi auto-connected to priority-100 `Actuate_5G`, Tailscale reconnected, tmux session respawned, Obsidian container restarted, Caddy came back, mDNS working, status page live). Hard-won lessons captured in the synthesis note and the scripts' README gotchas section. Key gotchas worth remembering: NM uses RFC-7217 stable-privacy link-local IDs by default (non-deterministic — phase-03 pins `ipv6.addr-gen-mode=eui64` so the direct-cable target stays stable); Ubuntu's NM only auto-manages wifi unless a `[device] managed=true` conf.d drop-in exists; Actuate_5G has AP/client isolation (wifi clients can't reach each other — Tailscale is the escape hatch); `sytone/obsidian-remote` bundles Obsidian from Oct 2022 and will prompt to auto-update, but the shipped AppImage is read-only so updates crash the X display — keep updates declined, extract a matching AppImage into `/config` if a newer version is needed; `ssh`/`scp`/`rsync` over scoped IPv6 link-local all need the bracket form `user@[fe80::x%iface]:path`; `((var++))` under `set -e` exits when var was 0. Memory file at `~/.claude/projects/-home-mork-work-local-network-scripts/memory/firebat-minipc-access.md` has the access table (SSH, web UI, dashboard URLs, login user `abc` not `mork` for the Obsidian container).
- **Earlier bootstrap KB note** — [[2026-04-23_firebat-minipc-network-setup]] captures the initial "HDMI dark, no idea what's on this box, what user do we log in as" recovery journey (direct-ethernet SSH via IPv6 link-local, GRUB `init=/bin/bash` rescue boot, password-SSH-to-byte-perfect-key-install dance). Prequel to the always-on synthesis.

### Post-wrap addendum — cleanup Lambda saga (23:00Z–00:15Z)

This session continued the §3 cleanup-Lambda rollout after the earlier wrap. Two PRs shipped end-to-end, the first real cleanup disable landed (for real, not silently), and a 14-day roster sweep proved the safety net's correctness.

- **§3 PR #7 merged — Immix 400 treated as gone, not transient.** Motivation: schedule `636be1ba` (admin_pk=235) emitted 9 transient errors over 24h with persistent `HTTP 400 + "Immix system is unavailable"` body. Persistence per-schedule (not fleet-wide) = not-transient. Treating 400 the same as 404 in `_check_immix`. Deploy succeeded; verified via ad-hoc synthetic invoke. [[2026-04-23_cleanup-rollout-day]] §22:09.
- **Silent PATCH-drop bug root-caused + fixed (PR #8).** First "cleanup disable" at 22:09:57Z for schedule 235 logged `disabled admin schedule_pk=235` but the admin UI still showed Active. Round-trip GET revealed PATCH was sending `{is_deleted, disabled_by, disabled_at}` — snake_case keys against a strict-camelCase serializer, plus `is_deleted` which isn't a serializer field at all. DRF silently dropped all three; PATCH returned 200 via `updated_date` auto-bump, zero business effect. User identified a cleaner fix than exposing `is_deleted`: PATCH `scheduleStatus="Deleted"` instead — triggers the view's existing `deploy_schedule_settings → model.deploy → should_undeploy → undeploy()` chain for the container teardown. PR #8 ships that + a post-PATCH GET verify checking all three target fields landed; raises `_TransientError` on mismatch so SQS-redrive → DLQ alarm takes over. Live-tested against 235 before merge. Deploy succeeded 23:01:37Z; 3-invoke synthetic chain at 23:03:26Z produced the first correctly-landing cleanup disable. Admin state for 235: `scheduleStatus=Deleted / disabledBy=cleanup_lambda / disabledAt=2026-04-23T23:03:26Z`.
- **Immix API error-pattern catalog written.** Separate KB concept note [[2026-04-23_immix-api-error-patterns]] — Immix deviates from REST (400 bodies look transient but mean "gone"). Documents what we handle, what we treat as transient that might be misclassified, and recommends future `_check_immix` instrumentation (structured log fields or NR custom event) so the next such pattern surfaces in aggregation queries instead of silent multi-day retry loops. Task #27 follow-up.
- **14-day roster sweep.** Collected all distinct schedule_ids processed in the last 14d (7 total). Checked admin state + forced-to-threshold via synthetic invokes where possible. Result: **exactly 1 schedule (235) was genuinely gone in Immix** — it's cleaned up. 3 others returned Active from Immix (anomaly-reset fired correctly, safety net working); 1 too far from threshold to force (site_disabled bucket); 2 not in admin at all (Lambda silently ACKs them). Side-signal: 4/5 admin-tracked schedules in the stage pool are in a "connector-sees-no-patrols / Immix-says-active" state-mismatch — worth revisiting via Task #25 anomaly-reset-rate monitoring.
- **Orphan container cleanup follow-up filed** — [connector_deployer#160](https://github.com/aegissystems/connector_deployer/issues/160), assigned to self. When cleanup Lambda sees `scheduleId` not in admin, it silently ACKs — but the connector pod for that schedule keeps running forever. 2 observed orphans in the sweep. Best fix is deployer-side: periodic orphan scan listing deployed containers, verifying `admin_pk` still exists in admin, DELETEing on 404. Cost + noise, not correctness.

**Handoff-note rule seeded into `/daily-wrap`.** Earlier this session a handoff note was lost between sessions, causing the next session to pick up the wrong carry-forward task. `daily-wrap` skill updated with Step 5.5 — carry-forward work must be persisted to the KB (attached to the workstream's concept/synthesis note or a new `YYYY-MM-DD_handoff-<slug>.md`) and wikilinked from mark-todos, not left only in chat transcript. Feedback memory saved.

## Closed Sub-items

*Granular `[x]` checklist items closed today, swept from `mark-todos.md` on 2026-04-27. Many of these are also captured in narrative form above; listed here so the §N workstream's checkbox history is queryable.*

**§3 — New Lambda — AutoPatrol stale-schedule cleanup (2026-04-23 hotfix + hardening pass + Step E flip):**

- [x] **#3a onboarder healthcheck hotfix** (PR #4) — downgraded silent-bail 404 return to `logging.warning`. Resolved [[2026-04-23_postmortem-onboarder-healthcheck|47h silent-failure incident]].
- [x] **#3b cleanup Lambda retry-idempotency fix** (PR #5) — DDB `ConditionExpression` on `last_message_id` prevents counter double-counting on SQS retries.
- [x] **#3c deploy workflow hardening** (PR #6) — fails on real AWS errors (not just ResourceNotFound), masks CodeArtifact token. Found via monitoring: prior workflow silently swallowed AccessDenied for 3 days, hiding partial deploys.
- [x] **#3d IAM policy v2** — added `lambda:UpdateFunctionCode` for cleanup + reenable ARNs in both regions. Root cause of partial deploys.
- [x] **#3e DLQ drained + pk=235 row reset** — pre-fix over-counted row deleted; DLQ emptied. Clean state pre-Step-E.
- [x] **Step E.1** (reframed as follow-up, task #19) — retry-idempotency fix organic exercise. Safety-net via anomaly-reset makes this a nice-to-have, not a gate.
- [x] **Step E.2** — `CLEANUP_ENABLED=true` flipped on cleanup Lambda us-west-2 at **2026-04-23T17:59:26Z**. Pre-flip acceptance state: 0 actual disables all-time, 0 would-PATCH all-time, 0 DLQ, dashboard GREEN.
- [x] **§3 PR #7 merged** — Immix HTTP 400 treated as "gone" (not transient). Schedule `636be1ba` (admin_pk=235) emitted 9 transient errors over 24h with persistent `HTTP 400 + "Immix system is unavailable"` body. Persistence per-schedule = not-transient. Treating 400 the same as 404 in `_check_immix`.
- [x] **§3 PR #8 merged** — Silent PATCH-drop bug. PATCH `scheduleStatus="Deleted"` (triggers existing deploy→undeploy chain) + `disabledBy`/`disabledAt` for audit + 3-field post-PATCH verify. First correctly-landing Lambda cleanup disable produced at 23:03:26Z (admin_pk=235).

**§6 — Software Architecture sketches (init bundle + first sketch):**

- [x] **Init + setup (parked → CLOSED)** — first commit `6f80028` of the scaffold on `main`; `datetime.utcnow()` deprecation fix landed across all 4 sites; `uv` migration; PyPI index persistence resolved via `uv.toml` + `--no-config` bootstrap.
- [x] **§6 sketch 1/5 — metrics collector SHIPPED** (commit `263cd1a`). Radon cyclomatic complexity collector across `vms-connector`: 239 files / 1,370 functions / ~0.5s; mean 3.62, p95 13, max 121; A/B/C/D/E/F rank split 1146/128/67/22/4/3; 0 parse failures. Top offender: `AnalyticsSiteManager._log_memory_breakdown` (cc=121). Findings note: [[2026-04-23_sketch-findings-metrics]].

**§9 — Operational Dashboard (Phase 1a complete):**

- [x] **Surface decision** — locked 2026-04-23 to **local static HTML** (`/home/mork/Documents/worklog/dashboard/` + per-day subdirs + latest symlink). No server. File-based per run. See [[2026-04-23_dashboard-sketch]] "Form" section.
- [x] **Phase 1 plan approved** — detailed plan at `/home/mork/.claude/plans/clever-tickling-swing.md` (2026-04-23). Split into 1a/1b. Additional metric categories added to catalog: CPU/memory per deployment type (user-priority), AWS Budgets + cost anomalies, NR APM golden metrics, K8s HPA + ArgoCD.
- [x] KB capture: 1a/1b scope in sketch + §9 + engineering-process _summary (Step 0 of plan)
- [x] `config/signals.json` full catalog (~60 signals; 2 enabled for 1a)
- [x] `config/baselines.json` static values (recalibrated 2026-04-23 after smoke-run surfaced wrong fingerprint)
- [x] Shared sink: `sink/.schema.md` + empty `sink/observations.jsonl` + `sink.py` helper (`write_observation()` + `read_recent()`)
- [x] Skill scaffold: `SKILL.md`, `run.sh` (system-python-first with venv fallback + public-PyPI override), `requirements.txt`, `README.md`
- [x] `render.py` orchestrator with regression rules 1/2/5 + sink reader for morning summary
- [x] Jinja2 templates (index + component + regressions + macros) + CSS (dark theme, self-contained)
- [x] Collectors wired inline in SKILL.md (bash blocks for 1a; dedicated collect_*.sh in 1b)
- [x] Tests: 10 pytest tests pass including `test_onboarder_silent_earlyreturn_is_red` (**canonical acceptance test** — the 2026-04-23 incident is caught by Rule 5)
- [x] One end-to-end signal wired: `onboarder_activity_us` × `onboarder_lambda_invocations_us` — both running against live CW
- [x] Smoke run: `/home/mork/Documents/worklog/dashboard/2026-04-23/index.html` viewable, overall GREEN, exit 0

## Closed Workstreams

### §9-old. AutoPatrol Alarm & Dashboard System — SUPERSEDED 2026-04-23

> **SUPERSEDED 2026-04-23** — this narrower AutoPatrol-scoped workstream was folded into the cross-repo §9 ("Operational Dashboard — cross-repo shorthand metrics"). The original sketch [[2026-04-23_alarm-dashboard-sketch]] remains the concrete AutoPatrol-specific seed; the generalized design is [[2026-04-23_dashboard-sketch]]. The cross-repo §9 Phase 1a shipped 2026-04-23 with the onboarder `activity_marker_antipattern` signal wired end-to-end. Archived here on the day it was superseded.

**Priority:** high — surfaced by 2026-04-23 onboarder incident
**Tickets:** *(no ticket yet — to be filed when planned)*
**Status:** design sketch written; superseded same day by cross-repo §9
**Trigger:** 2026-04-23 — silent early-return on onboarder Lambda broke new-schedule activation for ~2 days before a customer reported it. No alarm fired; no dashboard anomaly visible. Need a daily-checkable consolidated health surface.

#### Goal

Design + implement a consolidated alarm + dashboard surface for the AutoPatrol system so anomalies surface within hours (not days). Cover:
- Onboarder liveness (contracts fetched, sites/schedules onboarded per region)
- Cleanup Lambda pipeline health (DLQ, error rates, anomaly resets, DDB counter sanity)
- Connector emit flow (emit rate ↔ SQS delivery ↔ Lambda consume)
- Upstream Immix API health (indirect signal via our error rates)
- Alert rate anomalies (`#autopatrol-sync` Slack cadence)

#### Subtasks (pre-plan)

- [x] **Sketch design** — [[2026-04-23_alarm-dashboard-sketch]] — 5 metric families, 3 impl approaches (CW dashboards, NR-first, hybrid), dashboard mockup, 5 open questions for planner
- [ ] **Hand off to planner session** — pick up the sketch, produce a formal ExitPlanMode-style plan with scoped phases *(supplanted by cross-repo §9 plan)*
- [ ] **Phase 1: unblock by wiring onboarder to NR** (NR layer, custom events `AutoPatrolOnboarderInvocation`) — known gap since this project kicked off *(rolled into §9 Phase 2+)*
- [ ] **Phase 2: CloudWatch metric filters + dashboard** — cheapest + always-available surface. Start with the 5-metric dashboard mocked up in the sketch. *(rolled into §9 Phase 1a/1b)*
- [ ] **Phase 3: alarms + daily-review skill** — `/autopatrol-morning-check` that runs all queries in parallel + produces all-green/yellow/red output with auto-drill-down *(rolled into `/dashboard-check`)*
- [ ] **Phase 4: baseline calibration + threshold tuning** — 14 days of "informational only" mode before enabling page/Slack alerts *(rolled into §9 Phase 1b)*

#### Relevant KB

- [[2026-04-23_alarm-dashboard-sketch]] — design sketch (this workstream's starting point)
- [[2026-04-23_release-acceptance-criteria]] — upstream rule this alarm system operationalizes
- [[autopatrol-onboarder]] — onboarder entity
- [[autopatrol-cleanup-lambda]] — cleanup entity
- [[2026-04-20_cleanup-lambda-runbook]] — existing manual commands, many become alarm queries

#### Hand-off

Designated for a separate session to pick up. Sketch note has the 5-metric-family table + dashboard mockup; planner session should turn it into phased plan + ticket(s). **Superseded same day** — generalized into the cross-repo §9 dashboard initiative; this AutoPatrol-scoped workstream is preserved for traceability.

---

### §1 Inference API v5 — finish for testing

**Priority:** current focus
**Tickets:** ENG-126 (external-api initiative → v5 detection API for [[integrations/ebus/_summary|EBUS]])
**Status:** dev-complete; prod tier pending

#### What's left

- [x] Finish endpoint implementation — confirmed 2026-04-17 (motion-plus 500 fix closed the last known gap; see [[2026-04-17_v5-motion-plus-500-fix]])
- [x] Verify Swagger examples render correctly per model — `/docs` returned 200 via Basic Auth; verified per-model 2026-04-17
- [x] Verify per-model doc pages in `docs/api/v5/` — swept 2026-04-20; only real drift was `sliced-intruder-plus-with-vehicle.md` leaking "SAHI sliced inference" (reworded)
- [x] Strip "SAHI" library leak from externally-visible v5 code — PR #58 open against develop; 3 code spots (`registry.py` model description + `SlicedModelData` class docstring + `max_slices` field description; `v5.py` OpenAPI description for `GET /v5/models`) + 1 doc spot. Externally visible via `/v5/models` response and Swagger `/docs`. Pytest 36/36; pre-commit clean.
- [x] Hand off to EBUS for partner testing — **dev tier** key + E2E delivered 2026-04-17 (see [[ebus-partner-access]]); prod tier handoff pending
- [x] Sync `generate_api_key.py` role flags with `AcceptedRoles` in `check_api_key.py` — PR #57 merged to develop 2026-04-17 (side-effect of EBUS key generation surfacing the drift)
- [x] Run `/validate-release` pre-merge — retroactive for #56/#57/#58: CI green on all three; audited 2026-04-20. Going forward the skill should be invoked pre-merge — added to discipline list.
- [x] Post-push audit (security, RBAC, bounded inputs, 404 hints filtered by role) — audited 2026-04-20: #56 `FrameSizeMismatchException` echoes caller-supplied shapes (not a leak), 400 mapping clean; #57 generate_api_key.py CLI-tool only, no prod RBAC change (closes silent under-provisioning); #58 pure string rewording, no surface change. All pass.

**Archive note (2026-04-23):** dev-tier scope closed end-to-end; EBUS has a working dev key + E2E. Prod tier cut-over is a separate project that lives on outside §N bookkeeping — if it needs dedicated tracking, re-open as a new workstream at that point.

#### Relevant KB

- [[external-api/_summary|external-api topic]] — initiative context
- [[inference-api/_summary|inference-api topic]] — architecture
- [[2026-04-17_v5-motion-plus-500-fix]] — bug fix write-up (frame-size validation, exception-to-HTTP mapping)
- [[ebus-partner-access]] — dev-tier access record (DDB row, AG key ID, usage plan, rotation/revocation)
- [[partner-api-credential-runbook]] — generic recipe for minting partner keys (reusable for prod + other partners)
- Skill chain: `/api-endpoint-development` → `/write-external-docs` → `/validate-release`

## Notes / Learnings

- `/daily-scope` fan-out preflight gap surfaced: prod SSO was expired at the start of the fan-out, causing the Tier3 investigation to abort mid-run. Skill updated to add Step 2bb Preflight ahead of Step 2c Fan-out; memory `feedback_fanout_preflight.md` written; memory index updated.
- `/repo-scan` is now always-on in `/daily-scope` per user directive 2026-04-23. Description updated in both `/daily-scope` and `/repo-scan` SKILL.md files; memory `feedback_repo_scan_morning_default.md` written.
- CE category label "Replication/Lifecycle (Tier3)" is misleading in our account — 100% lifecycle, 0% replication. Worth flagging this anywhere the /cost-check output is surfaced (skill upgrade opportunity).
- CloudWatch `BucketSizeBytes` storage-class enumeration via `list-metrics` is a reliable churn-pattern fingerprint: a bucket with active writes but no storage-class dimension is almost always a short-TTL churn bucket. Promote this to a `/cost-check` enhancement.
- **Cross-session incident spillover folded in.** Sibling session ran the autopatrol_onboarder post-mortem ([[2026-04-23_postmortem-onboarder-healthcheck]]) + alarm-dashboard sketch ([[2026-04-23_alarm-dashboard-sketch]]). This session generalized those artifacts into a cross-repo workstream §9 in [[mark-todos]]: operational dashboard must cover vms-connector, libraries, admin, inference-api, alert pipeline — not just autopatrol. Two feedback memories cloned into this session's scope (`feedback_fail_fast_guards`, `feedback_acceptance_criteria_every_merge`). Three topic summaries updated with cross-refs: [[engineering-process/_summary]], [[fleet-architecture/_summary]], [[software-architecture/_summary]]. Engineering-process now carries two new principles (design-for-easy-monitoring; release-is-verified-not-declared). Fleet-arch now requires a "Monitoring & Alarms" subsection per proposal. Software-arch distinguishes code-health vs operational-health dashboards. Phase-0 signal inventory seeded for 2026-04-24 Morning Follow-Ups.

## Related

- [[mark-todos]] — scope picked from
- [[skill-daily-scope]], [[skill-daily-wrap]]
- [[2026-04-23_s3-tier3-cost-investigation]] — today's primary KB deliverable
- [[2026-04-23_s3-tier3-breakdown]] — cost data snapshot backing the synthesis
- [[2026-04-23_overnight-check]] — overnight health check (degraded — OOM surge is a separate pending task)
