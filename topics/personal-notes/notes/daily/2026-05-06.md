---
title: "Daily: 2026-05-06"
type: concept
topic: personal-notes
tags: [daily, wrap]
topics: [personal-laptop, engineering-process, operational-health, autopatrol, vms-connector, admin-api, new-relic, llm-shop, actuate-libraries, immix]
workstreams: ["§11", "§12", "§24", "§25", "§26", "§27"]
created: 2026-05-06
updated: 2026-05-06
author: kb-bot
---

# Daily: 2026-05-06

## Summary

Heavy connector + autopatrol day across two parallel tracks. **VCH stream_id history fix shipped lib→connector→stage→rearch** in a single day: actuate-libraries#344 published `actuate-pullers==1.17.17` (with backfill commit after the squash-body `[no ci]` markers silenced Publish Stable), vms-connector#1677 to stage validated cleanly (9 `stream_ids=[...]` events, 0 failed raises, ERROR-rate down), then promoted via #1679 stage→rearch. Sparked by a single patrol investigation (`A188AC1E…F17AF`) and ended up touching CLAUDE.md (dev-pin policy correction), surfacing a separate **Immix `streamfailed` artifact** that turns out to be a Worker-lifespan force-kill on their side — wrote a forwardable synthesis, mapped the full Vendor API via OpenAPI spec, and drafted an inquiry to Immix engineering since no partner-side fix exists.

**Cohort F closed end-to-end** by sibling session: 45 cids reframed into 22 Snowflake-ingestion (out of scope) + 23 real connector-side billing gaps. vms-connector#1675 (billing-emit invariant) addresses 210 of those cams; merged to stage. actuate_admin#2408 (`deactivate_customers_by_cids` mgmt command) drafted but no deactivations executed today. Discovery: US prod admin is ECS Fargate without `enableExecuteCommand`, forcing a runbook rewrite for `aws ecs run-task --overrides` instead of `kubectl exec`. Schedule-orphan-camera report shows 324 orphan cams across 56 customers (26%).

**§24 LLM shop** had its first source-control checkpoint — proxy rewritten to route by model name, mirrored 28 files into `local_network_scripts/files/llm-shop/`, smoke tests green.

§26.3 Snowflake export query DE-SCOPED (table-shape question never resolved); §26.5+6 dashboard signals deferred (gate condition needs revision now that §26.3 is dead); §27 + §3 Step F deferred to tomorrow.

## Closed Line Items

- **§26.1+2 — Cohort F classifier + per-customer tracker shipped to PR.** [autopatrol_onboarder#14](https://github.com/aegissystems/autopatrol_onboarder/pull/14) opened off fresh `feat/cohort-f-deep-classifier` branch off `master` (single commit `e1dbc3b`). Adds `scripts/ops/diagnose_silent_cameras.py` (Snowflake-vs-admin diff CSV → cohort A–F classifier; new `--cohort-f-deep` flag drills F into F1–F6 via 6 NRQL queries / customer with `(account_id, nrql)` cache) + `scripts/ops/cohort_f_tracker.json` (per-customer status/cause/ticket/owner with subgroup_history capped at 10; auto-bootstraps unknown F customers; preserves hand-edits across runs). Other 2 ops scripts (`immix_probe_deployer_failed.py`, `stale_schedule_scan.py`) intentionally left untracked. Read-only ops scripts; not part of the Lambda image.
- **VCH stream_id history + connect-failure raise iteration shipped to stage.** [actuate-libraries#344](https://github.com/aegissystems/actuate-libraries/pull/344) (merged 18:54Z) bumped `actuate-pullers` to 1.17.17: `AutopatrolWebSocketStreamPuller` now keeps a newest-first `stream_ids` list, atomically updated alongside `self.url` in `consume_stream`'s `ConnectionClosed` refresh path. [vms-connector#1677](https://github.com/aegissystems/vms-connector/pull/1677) (merged to stage 19:14Z, commit `3ee84734f`) bumped the pin and rewrote `vch_alert_sender.send_vch_failed` + `raise_alert` to walk `puller.stream_ids` newest→oldest and stop on first 2xx. Fixes a silent failure mode where the connect-failure raise hit Immix with a stream_id whose server-side session had been rotated out by the URL refresh, returning `400 $.streamId` — invisible since `vch_alert_sender` swallowed the exception. Sparked by the patrol `A188AC1E…F17AF` investigation; root-caused via the puller `ConnectionClosed` retry path and proven via 16 library + 12 connector tests. Stage validation in flight via release-chain-watcher; the money log line is `raise_patrol_alert {code} succeeded on stream_id candidate #N` (N>1). See [[2026-05-06_bugfix-stream-id-history-iteration]].
- **Immix `streamfailed` synthesis written for the partner conversation.** Separate from the bug above — every no-detection patrol shows `streamfailed` in Immix because their DeviceWorker has a 10-second hard life-span and is force-killed at expiry, which they label as failure. Their own log block (`Worker life span limit hit ... Force closing worker due to life-span expiry`) is the smoking gun. KB synthesis at [[2026-05-06_immix-streamfailed-worker-lifespan]] is structured for forwarding (internal-only sections clearly marked).
- **CLAUDE.md cleanup: dev pins are NOT allowed on stage.** User correction — stage now requires stable pins, matching rearchitecture. Removed the contradicting bullet, tightened the squash-merge gotcha note (don't even discuss `[no ci]` tokens explanatorily — GitHub Actions parses them as directives regardless), added TODO for proper custom-branch deployment story so dev-version validation can be decoupled from the stage branch's pyproject. Came up live today when PR #344's squash body inherited `[no ci]` markers from auto-bump commits and silenced Publish Stable; backfilled via direct push to `actuate-libraries` main with a clean-body `[patch:actuate-pullers]` commit (`3de7c30b`).
- **Cohort F investigation closed end-to-end.** NR billing-emit audit + Immix schedule-state spot-check across all 45 cohort F customers produced [[2026-05-06_cohort-f-investigation]] synthesis. Reframes the cohort: 22 cids / 392 cams ARE emitting `site_product_ended` to `event_queue_analytics.fifo` (Snowflake-side ingestion gap, downstream of SQS — out of connector scope) and 23 cids / 250 cams are real connector-side billing gaps. F3 splits 3 ways: F3a (9 cids / 102 cams Immix-Deleted, admin lagging), F3b (4 cids / 40 cams Paused — customer-controlled), F3c (4 cids / 33 cams Active in Immix with devices, idle on AWAITING). cohort_f_tracker.json hand-populated with 45 cids; pushed as follow-up commit `84255da` on PR #14 branch. Per-camera disposition CSV at `topics/autopatrol/notes/data/2026-05-06_silent-camera-disposition.csv` (1023 cams classified — 100% covered).
- **vms-connector#1675 — billing-emit invariant, MERGED to stage 18:27Z.** Closes the F4 errors + F3-idle billing gap (210 cams across 20 customers). Adds `startrun()` to `BaseConnectorFactory` mirroring `endrun()`; `AutoPatrolConnectorFactory.default()` calls `self.startrun()` at top + emits `site_product_ended` before each early-exit `exit(0)`; `AutoPatrolSiteManager.run()` exception handlers emit ended before each `exit(1)`. Cluster-wide finding pre-PR: 0 `site_product_started` events / 24h cluster-wide because the only call site (`base_healthcheck_camera.py:158`) was commented out — `_started` was just not in the connector's vocabulary. Post-merge: stage emits seen flowing in NR; release-chain-watcher subagent confirms first cron tick in flight. PR #1676 (over-cautious feature flag retrofit) closed without merging — user decided stage emits are fine. 6 unit tests (originally) + 11 (with the rejected flag patch) all passed locally.
- **actuate_admin#2408 — `deactivate_customers_by_cids` mgmt command (DRAFT).** Re-usable Django mgmt command for "Immix says gone, admin lagging" backfills. Calls `Customer.delete()` per cid; cascades cameras + EKS teardown + `site_deleted` event. Supports `--cids` / `--reason` / `--apply` (default dry-run) / `--limit` / `--allow-non-autopatrol`. Auto-restricts to autopatrol integration unless override. **9 unit tests pass locally.** Drafted because user decided not to deactivate any cids today — leaving sites as-is. Code review surface remains open. Same command unblocks both §25 Cohort B backfill (31 cids / 353 cams) and §26 F3a deactivate (9 cids / 102 cams) once policy gets sign-off.
- **Discovery: US prod admin is ECS Fargate with `enableExecuteCommand: false`** — `kubectl exec` is unavailable on US prod admin. Both the §25 Cohort B runbook and the new §26 F3a runbook needed correction. Updated path: `aws ecs run-task --overrides` against `prod_camera_admin` task definition; output via firelens → New Relic. Same path for any future Customer.delete-style backfill. Updated [[2026-05-05_cohort-b-backfill-runbook]] + wrote [[2026-05-06_cohort-f3a-deactivate-runbook]] using the new exec pattern.
- **Schedule-orphan-camera report — 324 orphan cams across 56 of 83 customers (26%).** Distinct from silent-cameras: per-customer report of admin-active cameras NOT in any associated AP schedule. Reasons split into 4 classes: `all-schedules-deleted` (160 cams / 25 cust, all cohort B), `partial-active` (115 cams / 25 cust — true partial coverage), `partial-paused-only` (32 cams / 4 cust), `no-schedules` (7 cams / 2 cust). Two CSVs at `topics/autopatrol/notes/data/2026-05-06_orphan-cameras-with-reason.csv` (per-camera with human-readable reason column) and `2026-05-06_schedule-orphan-cameras-v2-per-customer.csv` (per-customer aggregate). Cross-references back into the silent-cameras disposition: every silent cam has a single accountable disposition. User-validated example: cid=40512 ABC Liquor Store 9, 22 cams admin-active, 8 on schedule, 14 orphan — confirmed by direct admin UI cross-check.
- **§24 — LLM shop `/api/proxy/chat` rewritten to route by model name + first source-control checkpoint.** Drops the `backend` request field — caller picks a model, server picks the upstream. Centralized `SYCL_PORTS` dict in `harnesses/_status/server.py` (14b@8200, 7b@8201, 8b@8202) with three protocol adapters: `_stream_ollama` (NDJSON passthrough), `_stream_sycl` (OpenAI SSE → NDJSON, with `predicted_per_second` from llama.cpp's `timings` block), `_stream_npu` (one-shot). New `GET /api/proxy/routes` endpoint exposes the routing table to the playground UI. Playground HTML/JS rebuilt: backend dropdown removed, model dropdown grouped by `<optgroup label="iGPU (SYCL) | NPU | CPU (Ollama)">`, route hint shown under selector. **Source-control:** `~/llm-shop/` mirrored to `local_network_scripts/files/llm-shop/` (28 files: harnesses, dashboard, systemd, install scripts, pyproject + pi config). Pre-sync diff confirmed staging matched deployed; loss-less. Smoke tests on deployed `:8080/api/proxy/chat`: ollama (1.5b) 2.0s, sycl (14b) 5.5s + 5.46 tok/s server, npu (tinyllama) 2.6s, sycl streaming 14b emits tokens individually. See [[2026-05-06_model-routed-proxy]].
- **Immix Vendor API surface mapped + StreamFinished inquiry drafted.** Got the OpenAPI 3.0.1 spec (`vendor-api-for-immix-autopatrol.json`) for the partner API and confirmed definitively: 16 operations across 13 paths; **no stream-lifecycle write surface exists** in the partner spec. The `AutoPatrolActionType` enum from a separate Immix doc (`[Started, Finished, Raised, TimeoutCheck, Timeout, StreamUrlRequested, StreamPending, StreamStarted, StreamFinished, StreamFailed, AudioClipRequested, AudioClipFinished, AudioClipFailed]`) is not part of the partner spec — internal-only. Means the `streamfailed` labeling we observed in [[2026-05-06_immix-streamfailed-worker-lifespan]] cannot be fixed from our side; needs an Immix-side change (either expose a `POST .../actions` endpoint or derive `StreamFinished` from observed graceful close). Wrote three artifacts: entity note [[immix-vendor-api]] with full operation inventory + library-coverage table + `AutoPatrolActionType` gap section; archived spec at `topics/autopatrol/notes/data/2026-05-06_immix-vendor-api-openapi-spec.json`; outbound inquiry draft at [[2026-05-06_immix-streamfinished-inquiry]] ready to send (engineer-to-engineer register, two-question structure, send checklist + post-response branching plan).
- **PR #1679 — stage→rearchitecture promotion opened, your approval.** [vms-connector#1679](https://github.com/aegissystems/vms-connector/pull/1679) bundles 6 PRs into rearchitecture: VCH stream_id history fix (#1677, the headline), AutoPatrol billing-emit invariant (#1675), DW empty-body guard (#1671), CLAUDE.md billing-event heuristic (#1674), pullers chore bump (#1669), plus #1665. Library bump: `actuate-pullers` 1.17.14 → 1.17.17. Dev-pin scan clean. CHANGELOG entry added. Stage validation captured in PR body — 9 `stream_ids=[...]` history-accumulation events post-deploy, 7 `raise_patrol_alert ... succeeded` API-level wins, 0 failures, ERROR-rate down 13 → 7 in matched 30-min windows. Pre/post-merge release-chain-watcher confirmed pod health flat, no regressions.

## Closed Sub-items

*Granular `[x]` checklist items closed today, swept from `mark-todos.md` on 2026-05-06.*

**§26 — Cohort F subgroup investigation + §16 hardening tail:**

Original mark-todos sub-items, moved verbatim on close:

- [x] **§26.1+2 — Commit + PR for classifier + tracker** — [autopatrol_onboarder#14](https://github.com/aegissystems/autopatrol_onboarder/pull/14) opened. Branch `feat/cohort-f-deep-classifier` off `master`; commit `e1dbc3b` adds `scripts/ops/diagnose_silent_cameras.py` + `scripts/ops/cohort_f_tracker.json` only. Other 2 ops scripts left untracked.
- [x] **§26.1 follow-up — First end-to-end run** — completed via the broader Cohort F investigation today (see Closed Line Items). With `NEW_RELIC_USER_KEY` + `NEW_RELIC_ACCOUNT_ID` + Snowflake-derived cohort, ran `--cohort-f-deep` plus follow-on Immix `/Schedules` probes across all 18 tenants. Tracker hand-populated with 45 cids; pushed as commit `84255da` on PR #14 branch. Outcome: cohort F reframed (22/45 cids are Snowflake-side ingestion gaps, 23/45 are real connector-side billing gaps). [[2026-05-06_cohort-f-investigation]] is the synthesis.
- [x] **Cohort F subgroup classifier (NR-driven)** *(impl landed locally 2026-05-05, NOT yet committed/pushed — see local state below)* — extended `scripts/ops/diagnose_silent_cameras.py` with `--cohort-f-deep` flag that for each F customer runs 6 NRQL queries (K8sContainerSample existence/restart count + Log counts for no_patrols/error/patrol_summary/billing-emit) and bins into F1–F6 via a hierarchy (F1→F2→F4→F3→F5→F6). NR auth via `NEW_RELIC_USER_KEY` + `NEW_RELIC_ACCOUNT_ID` env vars. Per-customer subgroup + rationale + signal counts written to JSON + new "Cohort F deep-dive" section in markdown. Caches NR results keyed on `(account_id, nrql)` so re-runs are free. Help renders OK; syntax-checks clean. **Not run end-to-end yet** — need a fresh Snowflake CSV + NR creds.

  **Local state to pick up tomorrow:** `/home/mork/work/autopatrol_onboarder/scripts/ops/diagnose_silent_cameras.py` is modified in working tree but the entire `scripts/ops/` dir is untracked in autopatrol_onboarder (pre-existing condition — these ops scripts have never been checked in). Current branch `fix/onboarder-info-logs-visible` is for a different workstream — should NOT bundle. Recommended sequence: cut a fresh branch off `main` (e.g. `feat/cohort-f-deep-classifier`), `git add scripts/ops/diagnose_silent_cameras.py`, commit, push, open PR. The other 2 scripts in `scripts/ops/` (`immix_probe_deployer_failed.py`, `stale_schedule_scan.py`) are not Mark's work and should stay untracked unless a separate decision is made about them.
- [x] **Per-customer fix-tracker** *(impl + smoke-tested locally 2026-05-05, NOT yet committed/pushed)* — `cohort_f_tracker.json` at `autopatrol_onboarder/scripts/ops/` with schema `{customers: {<cid>: {name, subgroup, subgroup_history, status, cause, ticket, owner, notes, first_seen_silent, last_seen_silent, updated}}}`. Status enum: `undiagnosed | diagnosed | fix-in-flight | fixed | wont-fix | rolled-off-fleet`. `diagnose_silent_cameras.py --cohort-f-deep` now: (a) auto-bootstraps unknown F customers as `undiagnosed`, (b) updates `subgroup` + appends to `subgroup_history` (capped at 10 entries) each run, (c) preserves hand-edited fields verbatim (status/cause/ticket/owner/notes), (d) updates `last_seen_silent` each run, never touches `first_seen_silent`. Output integration: per-customer F-subgroup table now carries `status`, `ticket`, `owner` columns + a status rollup line. Smoke-tested with 2-run scenario including F1→F3 subgroup flip + hand-edit preservation; all assertions pass.

Closeout narrative:

- Cut fresh `feat/cohort-f-deep-classifier` branch off `origin/master` (the repo's default; not `main` — handoff note had it slightly off). `git add` was tight: only `scripts/ops/diagnose_silent_cameras.py` + `scripts/ops/cohort_f_tracker.json`; the other two `scripts/ops/` files (`immix_probe_deployer_failed.py`, `stale_schedule_scan.py`) and the orphan `CLAUDE.md` stay untracked.
- Single commit `e1dbc3b` (1002 insertions). Commit message scopes the work as ops-only / read-only / not part of the Lambda image, and points at the source synthesis.
- PR #14 opened against `master` with full test plan (5 acceptance bullets) + auth notes (`NEW_RELIC_USER_KEY` + `NEW_RELIC_ACCOUNT_ID` for `--cohort-f-deep`; admin token via existing `scripts/fetch_local_test_env.sh prod` flow).
- Implementation half closed; the end-to-end run with fresh Snowflake CSV stays open as its own §26 sub-item (carried forward in mark-todos).

**§11 — Firebat minipc follow-ups:**

- Decommissioned `~/bin/claude-run-skill.sh` from firebat and from the toolkit (`local_network_scripts/files/`). Updated `phase-10-sessions.sh` to no longer push it; `phase-11-verify.sh` now asserts the wrapper is *absent* (inverted check). README + phase-13 docstrings updated to reflect "no recurring `claude -p` on the box".

**§12 — Minipc dashboard app:**

Original mark-todos line, moved verbatim on close:

- [x] **§12i — Strip LLM narrative pass from minipc `/dashboard-check` cron** — CLOSED 2026-05-06. The cron flow had been de-LLM'd already (`~/bin/run-dashboard-check.sh` calls `run.sh collect|render` directly). Closeout work: removed the silent LLM-fallback path from `morning-prep.sh` (missing `~/bin/<skill>` now hard-fails rc=127) and decommissioned `~/bin/claude-run-skill.sh` from firebat + toolkit. `phase-11-verify.sh` now asserts the wrapper is *absent* — reintroducing it requires unwinding three places. Verified by running morning-prep.service: 5.5s CPU, all 3 scripts exit_code=0. Subsumes §11e.

Closeout narrative:

- Verified the de-LLM cron flow was already live on firebat. `~/bin/run-dashboard-check.sh` invokes `dashboard-check/run.sh collect|render` directly (no `claude -p`); morning-prep summary at `/logs/morning-prep-2026-05-06.summary.json` shows all 3 skills ran via `invoker=script` in ~50s wall time.
- Followed up by ripping the LLM-fallback path out of `morning-prep.sh` (a missing `~/bin/<skill>` now hard-fails with rc=127 instead of silently reverting to `claude-run-skill.sh`). Triggered morning-prep service post-change as proof: 5.5s CPU, all green.
- §11e (cronify-friendly refactor) and the laptop-side parts of §11d are now subsumed and can collapse — flagged for cleanup at next /todos-audit.

**§9 — Operational dashboard:**

- Added top-of-page nav bar to all four dashboard templates (`index`, `regressions`, `trends`, `component`) so http://mork-firebat/dashboard/ has a consistent jump bar to the other surfaces on the box (home · kb · ask · today · jira · repos · metrics · ops · admin · logs · status). New `site_nav` macro in `dashboard-check/render/macros.j2` mirrors the `<nav class=statusnav>` block from `gen-status-page.sh`; styled to harmonize with the dark theme (CSS at end of `dashboard.css`). Synced via `sync-claude-skills.sh --skill dashboard-check`; 21 tests pass on remote.

## Closed Workstreams

## Notes / Learnings

- Discovered that §12i was effectively done a while back but never closed in mark-todos. The remaining work was *defensive*: removing the silent LLM fallback path so a future missing-script accident can't re-introduce token spend. Net gain is policy-as-code, not behavior change.
- The "no recurring claude on the box" policy is now hard-coded in three places: (1) `phase-11-verify.sh` checks the wrapper is absent, (2) `morning-prep.sh` rejects any non-`script*` invoker, (3) the wrapper file itself is gone from the toolkit. Drift requires re-introducing all three.
- Cross-ref: [[2026-04-30_three-tier-routine-check-pattern]] (the policy this enforces) · [[2026-04-30_firebat-script-conversion-candidates]] (conversion log).

### Cohort F investigation — billing-emit + Immix probes

- **`site_product_started` is genuinely not in the connector's vocabulary** (cluster-wide 0 events / 24h). The only call site, `camera/shared/base_healthcheck_camera.py:158`, is commented out. The billing system runs on `site_product_ended` only — `act_a` discriminates between `'patrol'` (AP) vs `'healthcheck'` (VCH) so the same event covers both flows. Implication: when policy says "emit `_started`/`_ended` paired", the actual implementation needs to introduce `_started` from scratch, OR `_ended`-only is acceptable as long as it fires every cronjob run. Both paths are now in PR #1675 (started + ended both emit).
- **Connector early-exit code paths in `AutoPatrolConnectorFactory.default()`** silently bypass billing in three concrete cases: (1) `get_patrols()` exception after 3 retries, (2) `get_patrols()` non-200 response after 3 retries, (3) get_patrols succeeds but returns no patrol_id. Each calls `emit_no_patrols_signal` (cleanup-Lambda SQS) but not the billing path. Plus two unhandled-exception blocks in `AutoPatrolSiteManager.run()`'s camera-runner loop (`AttributeError` / generic `Exception` → `exit(1)`) bypass the success-path `_send_site_product_ended_events()`. All five sites now emit ended before exit per PR #1675.
- **Per-cid billing volume on F6 (ABC Liquor Stores) confirms the connector's billing path is healthy** — cid 40499 ABC Liquor Store 8 emits 208 `site_product_ended` events / 24h to `event_queue_analytics.fifo` (40 cams × 4 checks/day × ~retries). The "silent in Snowflake" classification is therefore Snowflake-side ingestion failure, not connector emit failure. 21 cohort-F cids in this bucket totalling 386 cams.
- **F3 cohort splits cleanly via Immix `/Schedules` probe.** No admin-side `cid → tenant_id` mapping in cache, so workaround was: enumerate all 18 Immix tenants via `/Contracts`, list each tenant's schedules across all statuses (`Active`/`Awaiting`/`Paused`/`Suspended`/`Removed`/`Deleted`), build global `scheduleId → (tenant_id, status)` map (389 schedules total). Then cross-ref the 27 schedule UUIDs covered by 17 F3 cids: 14 Deleted, 6 Paused, 5 Active, 2 NOT_FOUND. The Immix `/Schedules/{schedule_id}` lookup uses a `tenantId` HEADER (not URL path) per `actuate_integration_calls/autopatrol_api.py:163`. Sub-minute probe.
- **F3c "Active in Immix with devices, no AWAITING patrols" is not a bug.** 4 cids (Tampa Office CS, Central District, 2992 Sheppard, VCH 1) have schedules `Active` in Immix with non-empty device lists (5–13 devices), but the connector's get_patrols query (which defaults to `?scheduleStatus=AWAITING`) returns empty because no patrol is currently queued. `no_patrols` log fires but it's normal idle state. The fix is still the connector PR #1675 (bill on cronjob run regardless), not a schedule-state cleanup.
- **Schedule devices use Immix-side device IDs, not admin Camera pks.** `Schedule.devices[].deviceId` is e.g. `134619`; admin `Camera.id` is e.g. `401951`. No direct join in cached data; the orphan-cameras analysis matches by `camera_name == deviceName` (works because admin/Immix devices are 1:1 by name pattern in this domain). For broader analysis without cache, the schedule's `siteCameraCount` vs `scheduleCameraCount` admin-reported fields give the orphan-count delta directly without per-camera joins.

### Tooling + KB notes

- **Re-usable mgmt command pattern**: `deactivate_customers_by_cids` (PR #2408) handles ANY "Customer.delete via cid list" backfill. §25 Cohort B (31 cids / 353 cams) and §26 F3a (9 cids / 102 cams) both use the same command parameterized by cid list. Default dry-run + `--apply` opt-in + autopatrol-integration safety check + per-cid error continuation. 9 unit tests cover every branch including the rejected non-autopatrol case + per-cid exception continuation. Pattern works for any future "Immix says gone, admin lagging" cohort surfacing.
- **NR's RLIKE requires full-string match.** `WHERE container_name RLIKE '^connector-(40499|40507|...)'` returned 0 results until anchored with trailing `.*`. Costs less than building 45 LIKE-OR clauses for cohort fan-out queries, but the gotcha is silent (no error, just empty result set). Capture for future cohort-fan-out queries.
- **NR `capture()` regex with embedded quotes errors.** `capture(message, r"event_type': '(?P<evt>[^']+)'")` fails parse — the apostrophes confuse NRQL's tokenizer. Workaround: pre-bucket via `FACET CASES(...)` with `LIKE '%target_event%'` clauses, one per known event-type value. Less flexible but no escaping headaches.
- **Carry-forward decisions:** §3 Step F flag flip DEFERRED (sites stay as-is); PR #2408 DRAFTED (no deactivations today); PR #1675 PROPAGATES through stage→rearch→prod ungated (user judged stage emits are fine, no need for the over-cautious feature-flag retrofit in PR #1676 which got closed). §27 PR 1 (autopatrol group demotion) deferred to tomorrow — too many decisions today.
- **115 orphan_partial_active cams remain as the residual triage bucket** — per-customer add-to-schedule vs deactivate decisions. Top: cid 38711 CORPORATE SERVICES (20), cid 41265 Ace Hardware (15), cid 40502 ABC Liquor #4 (14), cid 37460 Tampa Office (10). Requires per-customer outreach (BOSSecurity / Eyeforce / etc account owners), not a code change.

### Immix integration learnings

- **`[no ci]` markers in PR squash-bodies silence `Publish Stable`**, even when discussed explanatorily — GitHub Actions parses them as directives regardless of context. PR #344's auto-bump commits leaked into the squash body and blocked the stable bump; backfilled via direct push to `actuate-libraries` main with a clean-body `[patch:actuate-pullers]` commit. CLAUDE.md tightened: don't even type the tokens in commit messages.
- **Concurrent session collision** clobbered uncommitted edits to `vch_alert_sender.py` + `test_streamid_null_guard.py` mid-session. A parallel Claude was on the same branch (`fix/vch-stream-id-iteration`) and switched away, taking the working tree with it. Re-applied from conversation context — but the lesson is **claim the branch via `/claim` before any non-trivial >15-min work on a tracked surface** (CLAUDE.md global rule, was not invoked here).
- **`AutoPatrolActionType` enum is internal-only.** Verified against the OpenAPI 3.0.1 spec: the partner Vendor API has 16 operations, 21 schemas, no `AutoPatrolActionType`, no audio-clip endpoints, no stream-state write surface. So any "stream lifecycle reporting" beyond `PatrolStatus` (Pending/Started/Finished) is Immix-side derivation, not partner-callable. The `streamfailed` labeling bug is therefore not fixable from our codebase — needs an Immix engineering ask. Inquiry drafted at [[2026-05-06_immix-streamfinished-inquiry]].
- **Atomicity fix > iteration safety net.** Stage validation showed 0 hits on the new `succeeded on stream_id candidate #N` log line — meaning the iteration helper hasn't had to fall through. Reading: with the puller now keeping `url` and `stream_id` synced atomically across `ConnectionClosed` refresh, every raise lands on the first (newest) id. The atomicity fix in the puller is doing the heavy lifting; the connector-side iteration is the belt-and-suspenders. Strongest possible signal: 9 ConnectionClosed-refresh events on stage (the scenario IS happening) + 7 successful raises + 0 failures.
